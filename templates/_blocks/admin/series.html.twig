<h2>{{ 'Series'|trans }}</h2>
<div class="admin__series">
    {{ pagination|raw }}
    <div class="header">
        <div class="series__poster"></div>
        {% if sort == 'id' %}
            {% if order == 'desc' %}
                <a class="series__id" href="{{ path('app_admin_series', {sort: 'id', order: 'asc'}) }}">
                    {{ 'Id'|trans }}
                    {{ ux_icon('iconoir:nav-arrow-down', {height: '16px', width: '16px'}) }}
                </a>
            {% else %}
                <a class="series__id" href="{{ path('app_admin_series', {sort: 'id', order: 'desc'}) }}">
                    {{ 'Id'|trans }}
                    {{ ux_icon('iconoir:nav-arrow-up', {height: '16px', width: '16px'}) }}
                </a>
            {% endif %}
        {% else %}
            <a class="series__id" href="{{ path('app_admin_series', {sort: 'id', order: 'desc'}) }}">
                {{ 'Id'|trans }}
            </a>
        {% endif %}

        {% if sort == 'tmdb_id' %}
        {% if order == 'desc' %}
            <a class="series__tmdb_id" href="{{ path('app_admin_series', {sort: 'tmdb_id', order: 'asc'}) }}">
                {{ 'TMDB Id'|trans }}
                {{ ux_icon('iconoir:nav-arrow-down', {height: '16px', width: '16px'}) }}
            </a>
        {% else %}
            <a class="series__tmdb_id" href="{{ path('app_admin_series', {sort: 'tmdb_id', order: 'desc'}) }}">
                {{ 'TMDB Id'|trans }}
                {{ ux_icon('iconoir:nav-arrow-up', {height: '16px', width: '16px'}) }}
            </a>
        {% endif %}
        {% else %}
            <a class="series__tmdb_id" href="{{ path('app_admin_series', {sort: 'tmdb_id', order: 'desc'}) }}">
                {{ 'TMDB Id'|trans }}
            </a>
        {% endif %}

        {% if sort == 'name' %}
        {% if order == 'desc' %}
            <a class="series__name" href="{{ path('app_admin_series', {sort: 'name', order: 'asc'}) }}">
                {{ 'Name'|trans }}
                {{ ux_icon('iconoir:nav-arrow-down', {height: '16px', width: '16px'}) }}
            </a>
        {% else %}
            <a class="series__name" href="{{ path('app_admin_series', {sort: 'name', order: 'desc'}) }}">
                {{ 'Name'|trans }}
                {{ ux_icon('iconoir:nav-arrow-up', {height: '16px', width: '16px'}) }}
            </a>
        {% endif %}
        {% else %}
            <a class="series__name" href="{{ path('app_admin_series', {sort: 'name', order: 'desc'}) }}">
                {{ 'Name'|trans }}
            </a>
        {% endif %}

        {% if sort == 'localized_name' %}
        {% if order == 'desc' %}
            <a class="series__localized_name" href="{{ path('app_admin_series', {sort: 'localized_name', order: 'asc'}) }}">
                {{ 'Localized name'|trans }}
                {{ ux_icon('iconoir:nav-arrow-down', {height: '16px', width: '16px'}) }}
            </a>
        {% else %}
            <a class="series__localized_name" href="{{ path('app_admin_series', {sort: 'localized_name', order: 'desc'}) }}">
                {{ 'Localized name'|trans }}
                {{ ux_icon('iconoir:nav-arrow-up', {height: '16px', width: '16px'}) }}
            </a>
        {% endif %}
        {% else %}
            <a class="series__localized_name" href="{{ path('app_admin_series', {sort: 'localized_name', order: 'desc'}) }}">
                {{ 'Localized name'|trans }}
            </a>
        {% endif %}

        {% if sort == 'number_of_season' %}
        {% if order == 'desc' %}
            <a class="series__seasons" href="{{ path('app_admin_series', {sort: 'number_of_season', order: 'asc'}) }}">
                {{ 'Seasons'|trans }}
                {{ ux_icon('iconoir:nav-arrow-down', {height: '16px', width: '16px'}) }}
            </a>
        {% else %}
            <a class="series__seasons" href="{{ path('app_admin_series', {sort: 'number_of_season', order: 'desc'}) }}">
                {{ 'Seasons'|trans }}
                {{ ux_icon('iconoir:nav-arrow-up', {height: '16px', width: '16px'}) }}
            </a>
        {% endif %}
        {% else %}
            <a class="series__seasons" href="{{ path('app_admin_series', {sort: 'number_of_season', order: 'desc'}) }}">
                {{ 'Seasons'|trans }}
            </a>
        {% endif %}

        {% if sort == 'number_of_episode' %}
        {% if order == 'desc' %}
            <a class="series__episodes" href="{{ path('app_admin_series', {sort: 'number_of_episode', order: 'asc'}) }}">
                {{ 'Episodes'|trans }}
                {{ ux_icon('iconoir:nav-arrow-down', {height: '16px', width: '16px'}) }}
            </a>
        {% else %}
            <a class="series__episodes" href="{{ path('app_admin_series', {sort: 'number_of_episode', order: 'desc'}) }}">
                {{ 'Episodes'|trans }}
                {{ ux_icon('iconoir:nav-arrow-up', {height: '16px', width: '16px'}) }}
            </a>
        {% endif %}
        {% else %}
            <a class="series__episodes" href="{{ path('app_admin_series', {sort: 'number_of_episode', order: 'desc'}) }}">
                {{ 'Episodes'|trans }}
            </a>
        {% endif %}

        {% if sort == 'origin_country' %}
        {% if order == 'desc' %}
            <a class="series__country" href="{{ path('app_admin_series', {sort: 'origin_country', order: 'asc'}) }}">
                {{ 'Country'|trans }}
                {{ ux_icon('iconoir:nav-arrow-down', {height: '16px', width: '16px'}) }}
            </a>
        {% else %}
            <a class="series__country" href="{{ path('app_admin_series', {sort: 'origin_country', order: 'desc'}) }}">
                {{ 'Country'|trans }}
                {{ ux_icon('iconoir:nav-arrow-up', {height: '16px', width: '16px'}) }}
            </a>
        {% endif %}
        {% else %}
            <a class="series__country" href="{{ path('app_admin_series', {sort: 'origin_country', order: 'desc'}) }}">
                {{ 'Country'|trans }}
            </a>
        {% endif %}

        {% if sort == 'first_air_date' %}
        {% if order == 'desc' %}
            <a class="series__first_air_date" href="{{ path('app_admin_series', {sort: 'first_air_date', order: 'asc'}) }}">
                {{ 'Release'|trans }}
                {{ ux_icon('iconoir:nav-arrow-down', {height: '16px', width: '16px'}) }}
            </a>
        {% else %}
            <a class="series__first_air_date" href="{{ path('app_admin_series', {sort: 'first_air_date', order: 'desc'}) }}">
                {{ 'Release'|trans }}
                {{ ux_icon('iconoir:nav-arrow-up', {height: '16px', width: '16px'}) }}
            </a>
        {% endif %}
        {% else %}
            <a class="series__first_air_date" href="{{ path('app_admin_series', {sort: 'first_air_date', order: 'desc'}) }}">
                {{ 'Release'|trans }}
            </a>
        {% endif %}

        {% if sort == 'status' %}
        {% if order == 'desc' %}
            <a class="series__status" href="{{ path('app_admin_series', {sort: 'status', order: 'asc'}) }}">
                {{ 'Status'|trans }}
                {{ ux_icon('iconoir:nav-arrow-down', {height: '16px', width: '16px'}) }}
            </a>
        {% else %}
            <a class="series__status" href="{{ path('app_admin_series', {sort: 'status', order: 'desc'}) }}">
                {{ 'Status'|trans }}
                {{ ux_icon('iconoir:nav-arrow-up', {height: '16px', width: '16px'}) }}
            </a>
        {% endif %}
        {% else %}
            <a class="series__status" href="{{ path('app_admin_series', {sort: 'status', order: 'desc'}) }}">
                {{ 'Status'|trans }}
            </a>
        {% endif %}

        {% if sort == 'provider_name' %}
        {% if order == 'desc' %}
            <a class="series__provider" href="{{ path('app_admin_series', {sort: 'provider_name', order: 'asc'}) }}">
                {{ ux_icon('fa6-solid:tv', {height: '18px', width: '18px'}) }}
                {{ ux_icon('iconoir:nav-arrow-down', {height: '16px', width: '16px'}) }}
            </a>
        {% else %}
            <a class="series__provider" href="{{ path('app_admin_series', {sort: 'provider_name', order: 'desc'}) }}">
                {{ ux_icon('fa6-solid:tv', {height: '18px', width: '18px'}) }}
                {{ ux_icon('iconoir:nav-arrow-up', {height: '16px', width: '16px'}) }}
            </a>
        {% endif %}
        {% else %}
            <a class="series__provider" href="{{ path('app_admin_series', {sort: 'provider_name', order: 'desc'}) }}">
                {{ ux_icon('fa6-solid:tv', {height: '18px', width: '18px'}) }}
            </a>
        {% endif %}
    </div>
    <div class="content">
        {% for s in series %}
            <a class="item" href="{{ path('app_admin_series_edit', {id: s.id}) }}">
                {% if s.poster_path %}
                    <div data-title="{{ s.name }}">
                        <img src="/series/posters{{ s.poster_path }}" class="series__poster" alt="{{ s.name }}">
                    </div>
                {% else %}
                    <div class="series__poster">
                        {{ ux_icon('fa6-solid:tv', {height: '18px', width: '18px'}) }}
                    </div>
                {% endif %}
                <div class="series__id">{{ s.id }}</div>
                <div class="series__tmdb_id">{{ s.tmdb_id }}</div>
                <div class="series__name">{{ s.name }}</div>
                <div class="series__localized_name">{{ s.localized_name }}</div>
                <div class="series__seasons">{{ s.number_of_season }}</div>
                <div class="series__episodes">{{ s.number_of_episode }}</div>
                <div class="series__countries">
                    {% for c in s.origin_country %}
                        <div class="series__country" data-title="{{ c|country_name }}">{{ getEmojiFlag(c) }}</div>
                    {% endfor %}
                </div>
                <div class="series__first_air_date">{{ s.first_air_date|format_date('short') }}</div>
                <div class="series__status">{{ s.status|trans }}</div>
                <div class="series__provider"{% if s.provider_logo %} data-title="{{ s.provider_name }}"{% endif %}>
                    {% if s.provider_logo %}
                        <img src="{{ s.provider_logo }}" alt="{{ s.provider_name }}">
                    {% endif %}
                </div>
            </a>
        {% endfor %}
    </div>
    {{ pagination|raw }}
</div>