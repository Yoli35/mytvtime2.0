{% extends 'base.html.twig' %}

{% block title %}my Tv Time→ {{ 'Updates'|trans }}{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/contact.scss') }}">
    <link rel="stylesheet" href="{{ asset('styles/user.scss') }}">
{% endblock %}

{% block body %}
    {% include('_blocks/_menu.html.twig') %}
    <div class="container-fluid user-profile-container"> {# ← light #}
        <div class="site-updates">
{#            <div class="toggle-theme">{{ ux_icon('mdi:sun-moon-stars') }}</div>#}
{#            <script>#}
{#                document.addEventListener('DOMContentLoaded', function () {#}
{#                    const toggleTheme = document.querySelector('.toggle-theme');#}
{#                    toggleTheme.addEventListener('click', function () {#}
{#                        document.querySelector(".container-fluid").classList.toggle('light');#}
{#                    });#}
{#                });#}
{#            </script>#}
            <div class="site-update glowing-border">
                <div class="updates-title">
                    <h1>Mises à jour / Updates / {{ 'Updates'|trans(domain = 'messages', locale = 'ko') }}</h1>
                    <h4>myTvTime 2.0 : 18/09/2024</h4>
                </div>
            </div>
            <div class="site-update glowing-border">
                <h2>48ᵉ mise à jour : 17/11/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:language-javascript') }}→ Replace text references with translations in Season.js</div>
                        <div class="details">Updated all occurrences of gThis.text to gThis.translations for setting data-title and placeholder attributes. This change improves internationalization support by ensuring UI strings are sourced from the translations object.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add outline styling to series and user components</div>
                        <div class="details">Introduces a 4px solid outline using a semi-transparent ardoise color to various flex containers in series.scss and user.scss for improved visual distinction and consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add season localized overview API and refactor forms</div>
                        <div class="details">Introduces ApiSeasonOverview for managing season localized overviews via API. Refactors JS and Twig templates to support season-specific translation forms, updates entity constructors for stricter typing, and centralizes translation strings for season pages. Improves handling of SVG icons and conditional UI logic for translation tools and forms.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Show added date in series details</div>
                        <div class="details">Displays the date when the series was added alongside the visit count in the series details view for improved context.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:menu') }}{{ ux_icon('mdi:tv') }}→ Add premiere indicator for first episode of season</div>
                        <div class="details">Introduces a 'premiere' flag in EpisodeExtensionRuntime for episodes where seasonNumber and firstEpisodeNumber are both 1. Updates the menu block to display a premiere icon when this condition is met.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Focus and select URL input on form show</div>
                        <div class="details">When displaying the form, the URL input is now both focused and its contents selected for easier editing.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Set default backdrop path for series if missing</div>
                        <div class="details">Assigns the first available backdrop image to the series if 'backdrop_path' is null and backdrops exist, ensuring a fallback image is displayed.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:home') }}{{ ux_icon('mdi:filmstrip-box') }}→ Show last viewed movies on home page</div>
                        <div class="details">Added retrieval and display of the user's last viewed movies on the home page. Updated UserMovieRepository with a method to fetch recently viewed movies, adjusted HomeController to pass this data, and updated templates and styles for proper rendering. Added French translation for 'Last viewed movies'.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor episode info form event handlers</div>
                        <div class="details">Moved inline event handler logic for episode info form actions (copy switch, copy, cancel, submit) into dedicated class methods for better code organization and maintainability.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add bulk copy feature to episode info form</div>
                        <div class="details">Introduces checkboxes to select episodes for copying their title and overview, a 'Copy all' switch, and a button to copy selected data to the clipboard. Updates related JavaScript, SCSS for styling, and French translations. Also refines dialog and form button selectors and improves vote average display logic.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Cap vote average display at 10+ in series view</div>
                        <div class="details">Updated both the Season.js and _votes.html.twig template to display '10+' if the calculated vote average exceeds 10, ensuring consistent and user-friendly output for unusually high averages.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor cast display and improve cast UI</div>
                        <div class="details">Renamed _people.html.twig to _cast.html.twig and updated all references to use the new template for cast display. Enhanced cast card styling with larger border radii and a new 'added' state. Changed focus behavior in Show.js to target the character name input. Updated SeriesController to set order to -1 for added cast. Added a new blurred poster image.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:bug-outline') }}→ Update Doctrine config and fix datetime format in admin tools</div>
                        <div class="details">Replaces deprecated Doctrine ORM options with updated equivalents and adds controller_resolver auto_mapping. Also corrects the datetime-local input value format in the admin tools Twig template.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Refactor movie card styles and improve movie page layout</div>
                        <div class="details">Extracted card styles into a new card.scss file and removed duplicate card styles from home.scss. Enhanced movie.scss with improved layout, hover effects, and visual refinements for movie details, posters, and backdrops. Updated movie card Twig templates to use new data attributes and structure. Refactored movie show and tmdb pages to improve backdrop handling, user actions, and overall layout consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Comment out PosterHover imports and usage</div>
                        <div class="details">Temporarily disables the PosterHover feature by commenting out its import and initialization in app.js, as well as its entry in importmap.php. This may be for debugging, refactoring, or to address issues related to PosterHover.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:bug-outline') }}→ Update twig/intl-extra version constraint</div>
                        <div class="details">Changed the version constraint for twig/intl-extra from ~3.8.0 to ^3.0 in composer.json to allow for a wider range of compatible versions.
                            version: 3.8.x → 3.22.0</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Update episode card layout and styles</div>
                        <div class="details">Refactored the episode card template and SCSS to improve layout and visual grouping. Added icons for air date and watched status, adjusted flex and font styles, and separated air date and watched information for better clarity.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:menu') }}{{ ux_icon('mdi:tv') }}→ Track episode IDs for improved menu updates</div>
                        <div class="details">Refactored episode tracking to use episode IDs instead of series IDs in menu updates and data attributes. Updated backend and Twig logic to aggregate and expose episode IDs, ensuring accurate episode count and selection in UI components.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Fix locale handling for episode overview updates</div>
                        <div class="details">Episode overview updates now correctly use the request locale when querying and creating EpisodeLocalizedOverview entities, ensuring proper localization support.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add season and order badges for improved UI clarity</div>
                        <div class="details">Introduces distinct badges for season numbers and season order in the UI, updates related CSS for styling, and refactors JS and templates to use new class names. Enhances visual clarity and user interaction for season navigation and watch links.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>47ᵉ mise à jour : 09/11/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:bug-outline') }}→ Fix broadcast date lookup in SeriesSeasonEpisodes</div>
                        <div class="details">Replaces usage of local variable, falsely masked by the use of '??', with class property for broadcast date array, ensuring correct episode air date assignment.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add removal of episode name and overview if empty</div>
                        <div class="details">When updating an episode's substitute name or localized overview, the entry is now removed if the new content is empty. Repository classes for EpisodeSubstituteName and EpisodeLocalizedOverview now include a remove method to support this behavior.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:code') }}→ Refactor SeriesSeasonEpisodes and episode card layout</div>
                        <div class="details">Refactored SeriesSeasonEpisodes.php to improve code structure by extracting logic into private methods and using class properties for shared data. Updated SCSS and Twig templates to adjust episode card layout, increase still image height, and reorganize info lines for better display and maintainability.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Dynamic vote color styling for episode cards</div>
                        <div class="details">Adds automatic color detection for provider logos and applies dynamic background and text colors to episode vote elements. Updates SeriesSeasonEpisodes API to compute and pass color values, modifies SCSS for vote styling, and updates Twig template to use these colors for improved visual integration.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add episode runtime display to season episode cards</div>
                        <div class="details">Episode runtime is now retrieved and displayed in the season episode card if available. SCSS and Twig templates updated for styling and layout, and backend logic improved to fetch runtime from localized or fallback data.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:bug-outline') }}→ Fix getAlternateSchedule parameter to use seasons data</div>
                        <div class="details">Changed the call to getAlternateSchedule to pass only the 'seasons' data from the TMDB response instead of the entire TV object. This ensures the method receives the expected data structure.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve episode card styling and tooltips</div>
                        <div class="details">Updated SCSS to refine font sizes and background colors for season and episode cards. Enhanced HTML to provide better tooltip data by using episode name when overview is missing, moving watched date tooltip to episode number, and improving provider logo tooltip handling.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor quickLinks padding logic in SeriesController</div>
                        <div class="details">Moved the logic for adding an empty quickLink to ensure even count from before the itemPerLine calculation to inside the else block. This ensures padding is only added when necessary for layout purposes.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Enhance episode card with watch status, provider, and vote</div>
                        <div class="details">Updated the episode card UI and backend to display watch status, streaming provider logo, and user vote for each episode. The SeriesSeasonEpisodes API now aggregates and returns additional episode metadata, and the SCSS and Twig templates have been updated to reflect these new features visually.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:api') }}→ Update episode API, UI behaviors, and translations</div>
                        <div class="details">Refactored episode height API endpoint in Season.js and improved scrollIntoView behavior. Adjusted event handler and removed unused variable. Increased max-width for modal without images in app.scss. Standardized date formatting for episode queries and updated method signatures for type safety. Improved timezone handling in SeriesService. Added new French translations for missing image and crew roles.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add fallback to series poster if season poster missing</div>
                        <div class="details">If a season poster is not available, the template now displays the series poster as a fallback before showing the 'No poster' message.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Reorder provider and name fields in watch link form</div>
                        <div class="details">Moved the 'Provider' and 'Name' input fields below the season/episode hidden fields in the watch link form for improved form structure and logical grouping. And set focus to the url input.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add dynamic episode cards with stills to series seasons</div>
                        <div class="details">Introduces an API endpoint and supporting frontend logic to fetch and display episode cards with still images for each season in a series. Adds new styles for episode cards, updates the series show template to include the episode cards container, and fixes typos in DateService method parameters. Also adds a Twig partial for rendering individual episode cards.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Update season and info card styles for responsiveness</div>
                        <div class="details">Increased border-radius for season and info elements, refactored .show-tab styles for better hover effects, and improved mobile responsiveness by adjusting flex direction, border-radius, and hiding cast buttons on small screens.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:home') }}→ Refactor home page series status and TMDB movie display</div>
                        <div class="details">Improves the layout and structure of the series status section on the home page for better clarity and visual hierarchy. Updates HomeController to adjust provider selection and refines TMDB movie selection logic, removing unused video and provider data. Enhances SCSS for new status and TMDB card layouts, and disables the home page video background block.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:home') }}→ Add series status summary with percentages on home page</div>
                        <div class="details">Enhanced the series status section for logged-in users by displaying a summary with total count and percentage breakdowns for each status. Updated SCSS for improved layout and styling, including new background colors and responsive adjustments. Added translation for 'Total' in French.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:code') }}→ Refactor SeriesController to use SeriesService</div>
                        <div class="details">Moved multiple methods from SeriesController to SeriesService, reducing controller complexity and improving separation of concerns. Updated method calls and dependency injection accordingly. Also made minor style and variable improvements in series.scss and templates.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>46ᵉ mise à jour : 03/11/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:code') }}→ Standardize template file naming to snake_case</div>
                        <div class="details">Renamed all Twig template files and their references from kebab-case to snake_case for consistency. Updated all PHP controllers and template includes to use the new naming convention. This improves maintainability and reduces confusion caused by mixed naming styles.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Update episode list (provider, device & vote) border radius and spacing</div>
                        <div class="details">Adjusted the border-radius for the episode list to have rounded ends and moved the gap property to the .select-provider context. Also added specific border-radius for the first child and updated separator margin for improved UI consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Adjust border radius and padding in series.scss</div>
                        <div class="details">Increased the border radius for .quick-episode and .future-episode first/last classes to 5rem for a more rounded appearance. Updated padding for .user-actions child divs to improve layout consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve series status display and season layout</div>
                        <div class="details">Refactored the series status badge to a more prominent position in the user actions block and updated its styling for better visibility. Enhanced season poster layout with aspect ratio, larger card height, and improved font sizing. Added French translation for 'New schedule' and included a data-title attribute for accessibility. Cleaned up redundant status display in key facts.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor series page layout and update styles</div>
                        <div class="details">Reorganizes the series detail page by introducing 'block-infos' containers for better structure and modularity, moving posters, backdrops, logos, keywords, and user actions into their own blocks. Updates SCSS to support the new layout, adds improved styling for keywords and posters, and adjusts selectors for user actions. This refactor enhances maintainability and visual consistency across the series page.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Show season air date if not yet released</div>
                        <div class="details">Added logic to display the season air date in a styled element when the season has not yet aired, instead of showing the votes block. Introduced helper methods in SeriesController to determine and format the season air date, updated the template to conditionally render the air date or votes, and added corresponding styles.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve episode handling and cleanup in SeriesController</div>
                        <div class="details">Refactored logic to better handle 'finale' episodes, ensuring episodes after a finale are skipped when syncing user episodes. Removed unused imports and commented code, and improved the logic for updating series overviews and episode counts. Also adjusted the 'up_to_date' calculation to account for series with zero aired episodes.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:user-outline') }}→ Add access control to PeopleController</div>
                        <div class="details">Applied the #[IsGranted('ROLE_USER')] attribute to the PeopleController to restrict access to users with the ROLE_USER role.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve finale date display in schedules form</div>
                        <div class="details">Refactored the finale date element from a span to a div for better styling control, updated SCSS to style the finale date with a green background and transition, and adjusted JS to wrap the date in a span for opacity transitions. This enhances the visual feedback when a finale date is present.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add day selection sync in schedule form</div>
                        <div class="details">The controller now returns the day index when converting date and time, and the frontend updates the corresponding day checkbox based on the returned value. Also includes minor code cleanups in timezone menu generation and option rendering.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refine watch links UI and update series styles</div>
                        <div class="details">Removed extra padding from watch link buttons and adjusted SVG sizing for better alignment. Added a small label with an icon to the watch links block. Fixed stylesheet load order in season.html.twig to ensure correct CSS application.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:language-css3') }}→ Enable glow animation and update border radius in user.scss</div>
                        <div class="details">Uncommented the glow animation for elements with the glowing-border class and reduced the border radius for odd and even children from 15rem to 5rem for a more subtle rounded effect. Also added the 45th update entry to updates.html.twig, detailing recent features and improvements.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>45ᵉ mise à jour : 30/10/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:administrator') }}{{ ux_icon('mdi:tv') }}→ Show add cast button only for admins</div>
                        <div class="details">Wrapped the add cast button and search include in a check for the ROLE_ADMIN role, so only admin users can see and use these controls.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ TMDB series infos may have no network</div>
                        <div class="details">Correct an issue when series comes with no networks infos.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor watch-links and add label-small styling</div>
                        <div class="details">Reorganized the .watch-links structure under a .wrapper in series.scss and introduced a .label-small class for improved label handling. Updated user.scss to style .label-small and its SVGs. Also fixed the order of stylesheet imports in show.html.twig to ensure correct CSS precedence.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }}→ Adjust heights for map center and sidebar panels</div>
                        <div class="details">Updated the .center class to use a fixed height of calc(50vh - 4rem) and modified the sidebar panel height to calc(100% - 2rem) for improved layout consistency and responsiveness.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Change delete button type to button in localized name form</div>
                        <div class="details">Updated the 'Delete' button in the localized name form to use type="button" instead of type="submit" to prevent unintended form submissions when deleting.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Update episode range logic to use watch dates</div>
                        <div class="details">Changed the logic for determining the range of watched episodes from using episode IDs to using watch dates. This affects how episodes are retrieved and counted for binge detection.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}{{ ux_icon('mdi:filmstrip-box') }}→ Refactor translation and overview endpoints to API routes</div>
                        <div class="details">Moved movie and series name/overview add/remove logic from controllers to new API controllers under /api endpoints. Updated JavaScript to use new API routes. Added remove methods to MovieAdditionalOverviewRepository and MovieLocalizedOverviewRepository. This improves code organization and separates API logic from web controllers.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor episode API endpoints to /api and move logic</div>
                        <div class="details">Updated frontend fetch calls in Season.js to use new /api/episode and /api/series/episode endpoints. Moved all episode-related API logic from SeriesController to a new ApiSeriesEpisode controller, cleaning up SeriesController and improving separation of concerns.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:user-outline') }}{{ ux_icon('mdi:tv') }}→ Add character name support to series cast management</div>
                        <div class="details">Introduces a character name field to the SeriesCast entity, updates the repository to fetch this data, and modifies the UI to allow input and submission of a character name when adding a cast member. Also updates styles and translations to support the new input field.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:user-outline') }}{{ ux_icon('mdi:tv') }}→ Add SeriesCast entity and integrate cast management</div>
                        <div class="details">Introduces the SeriesCast entity and repository to manage series and season cast members. Updates Series and People entities with SeriesCast relations. Modifies SeriesController to support adding cast members, updating flash messages, and merging custom cast data with TMDB data. Updates styles and templates for cast addition UI, and adds relevant French translations.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:user-outline') }}→ Fallback to English biography if local one is missing</div>
                        <div class="details">When a person's biography is empty, attempt to use the English translation from TMDB translations if available. This improves the display of biographies for people lacking localized content.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:user-outline') }}{{ ux_icon('mdi:tv') }}→ Add cast search and UI for series show page</div>
                        <div class="details">Introduces a cast search input and related UI for adding people to a series cast on the show page. Updates JavaScript to initialize and handle cast search interactions, adjusts controller to handle flash messages for cast addition, and refines SCSS and Twig templates for the new UI elements.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:user-outline') }}{{ ux_icon('mdi:tv') }}→ Add UI and backend for adding cast members to a season</div>
                        <div class="details">Introduces a new UI component for searching and adding people to a series season's cast, including new Twig partials, updated styles, and JavaScript integration. Adds a backend route and controller action for handling cast addition (currently not fully implemented, shows a flash message). Updates translations and refactors season page templates to use the new cast button block.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add backdrop and overview to favorite series cards</div>
                        <div class="details">Updated UserSeriesRepository to include backdrop_path and overview in favorite series data. SeriesController now saves backdrop images in addition to posters. The favorite series card template was updated to include a hidden backdrop image and use the overview as a data attribute.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }}→ Refactor map help and unify map styles</div>
                        <div class="details">Extracted map keyboard help into a reusable Twig partial and included it in map pages. Moved and consolidated map-related SCSS from series.scss to map.scss for better organization and maintainability. Updated map location info markup for improved structure and consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:photo-library') }}→ Improve diaporama fullscreen and close behavior</div>
                        <div class="details">Added preventDefault in fullscreen handler and refactored closeDiaporama to handle fullscreen exit more robustly. Also added pointer cursor to album images for better UX.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Adjust album layout and improve responsiveness</div>
                        <div class="details">Updated height calculations for main and album containers to use 10rem instead of 13rem. Added flex-shrink and max-height properties to .photos and .last-days for better layout control. Refactored .album and .name styles for improved appearance and accessibility. Enhanced responsive design for .photos-page at smaller screen widths.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:photo-library') }}→ Refactor diaporama to use fullscreen div</div>
                        <div class="details">Replaces the dialog element with a div for the diaporama and utilizes the Fullscreen API for image display. Removes unused help and navigation code, streamlining the diaporama logic and improving user experience.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>44ᵉ mise à jour : 27/10/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}{{ ux_icon('mdi:heart') }}→ Add user favorites series page and menu link</div>
                        <div class="details">Introduces a new route and controller action to display a user's favorite series, adds a repository method to fetch favorite series, and creates corresponding Twig templates for the favorites page and series cards. Updates the menu to include a link to the favorites page and adds a translation key for 'Series.favorites'.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Update logic for displaying season finale label</div>
                        <div class="details">Simplifies the condition for showing the 'Season finale' label by checking only the episode type. Removes the previous logic that also checked episode number and season count.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Extend vote range and adjust vote list width</div>
                        <div class="details">Increased the vote options from 10 to 12 in the Season.js component and updated the max-width of the vote list in series.scss to accommodate the additional options.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:api') }}→ Refactor API admin tool to load modules via AJAX</div>
                        <div class="details">Moved API modules form markup to a new Twig partial and now load it dynamically via a new /admin/api/modules endpoint. Updated the JavaScript to fetch module data and render the form asynchronously, simplifying the main API admin route and removing embedded JSON from the page.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve season controls UI and update styles</div>
                        <div class="details">Added separator elements and adjusted layout in the season controls section of the series season template. Updated SCSS to style new separators, improve button interactivity with cursor changes, and refine padding and color for better visual clarity. Also updated user label color for improved contrast.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:api') }}→ Add admin API explorer tool with UI and translations</div>
                        <div class="details">Introduces a new admin API explorer accessible from the admin panel, including a new route, controller logic, Twig block, JavaScript for UI interactivity, and related SCSS updates. Adds French translations for API-related terms and endpoints, and updates the admin navigation and layout to support the new tool.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Skip episodes after finale in season processing</div>
                        <div class="details">Added logic to skip processing episodes that come after a finale episode in the season. Also added a debug dump for season episodes to assist with troubleshooting.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Fix watch link title field and add translation key</div>
                        <div class="details">Update WatchLink API to use 'name' instead of 'title' from request data. Add 'Watch on' translation key in MovieController.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:menu') }}→ Update menu for unauthenticated users and add translation</div>
                        <div class="details">Refactored the menu block to display a message and login/register links for users who are not authenticated. Added the French translation for 'Please log in to see more options' in messages.fr.yaml. Cleaned up commented code and improved menu structure.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:language-css3') }}→ Update TMDB logo path and improve series edit layout</div>
                        <div class="details">Changed the TMDB logo image path to '/images/interface/the-movie-database.png' in both movie and series edit templates. Refactored the series edit template to better structure the title and abstract sections for improved readability and maintainability.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:api') }}→ Refactor TMDBService to remove api_key from requests</div>
                        <div class="details">Updated TMDBService to stop appending the api_key parameter to TMDB API requests, centralizing request construction in a new getResults() method. Adjusted controllers to build query strings without leading ampersands and updated getMovieExtras/getSeriesExtras to accept a single params string. Also commented out unused TV keywords/external IDs code in SeriesController.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:menu') }}{{ ux_icon('mdi:map-outline') }}→ Fix menu block and update map stylesheets inclusion</div>
                        <div class="details">Corrects misplaced Twig endif in the menu block template. Comments out conditional inclusion of Leaflet and Mapbox stylesheets in the map index template, leaving only the main Mapbox stylesheet active.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve series page styles and status display</div>
                        <div class="details">Adjusted JS to update text color for season info blocks based on image color lightness. Tweaked SCSS for lighter background on fact and info blocks, enhanced styling for 'status-returning', and removed explicit color from .infos. Commented out 'watched' status CSS in SeriesController and simplified status display in the series show template.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }}→ Refactor map style selection and rendering logic</div>
                        <div class="details">Moved map style toggler logic from Twig template to Map.js for better separation of concerns. SeriesFilmingLocations API now renders the map block server-side and returns it in the response, which is injected client-side. Cleaned up Season.js to use the new mapBlock and removed redundant DOM manipulation. Twig template now conditionally renders map style list only if styles are present.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }}→ Refactor map initialization and add padding to gallery</div>
                        <div class="details">Split map control and marker initialization into separate functions for better readability and maintainability in Map.js. Also, added horizontal padding to the .gallery-list class in map.scss for improved layout.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>43ᵉ mise à jour : 19/10/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add POI toggler to map in season view</div>
                        <div class="details">Introduces a 'Points of Interest' toggler button to the map in the season view. Updates the JavaScript and Twig template to support the new UI element, including translation support and SVG icon integration.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add scale parameter to episode votes display</div>
                        <div class="details">Introduces a 'scale' parameter to the episode votes block for more flexible vote bar sizing. Updates all includes of the votes block to pass the appropriate scale value, improving visual consistency between season and show views.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Set next episode to null when progress is 100%</div>
                        <div class="details">Ensures that when a user's viewed episodes match the total episode count and progress is set to 100%, the next user episode is set to null. This prevents inconsistencies in episode tracking when a series is fully watched.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor episode votes UI and remove next/last episode tabs</div>
                        <div class="details">Simplifies the episode votes display by adjusting vote bar heights and background styles, and removes the next/last episode preview tabs from the series view. Also adds JS logic to hide user votes on mouse leave for improved UX.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Handle locations with only season number in series view</div>
                        <div class="details">Added a conditional to display the season number when the episode number is missing in the series/show template. This improves clarity for locations that reference a whole season rather than a specific episode.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Update series page layout and episode vote styles</div>
                        <div class="details">Adjusted header and poster layout for better responsiveness, modified vote graph and episode vote styles for improved visual clarity, and updated padding and positioning for several elements. Removed redundant and commented-out styles to streamline the stylesheet.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }}→ Add POI display toggle to map interface</div>
                        <div class="details">Introduces a 'Points of Interest' toggler button to the map UI, allowing users to show or hide POI markers. Updates JavaScript to handle toggling, adds relevant styles, modifies the map container template, and provides French translations for new UI labels.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve handling of missing series posters</div>
                        <div class="details">Adds distinct styling and icon for missing posters in series view. Updates ImageService to handle null poster paths gracefully. Enhances user feedback and visual consistency when a poster is unavailable.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve episode sync and management in series</div>
                        <div class="details">Enhanced SeriesController and UserEpisodeRepository to better handle addition, removal, and update of episodes when syncing with series data. Added user feedback for episode changes, improved error handling in vote display, and updated French translations for new messages and roles.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Update keyword block selector in Keyword.js</div>
                        <div class="details">Changed the DOM selector from '.keyword-block' to '.keywords' for displaying fetched keywords and initializing tooltips. Ensures correct element is targeted for keyword display and tooltip functionality.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Adjust lightness threshold for text color</div>
                        <div class="details">Changed the lightness threshold from 180 to 160 for determining the text color in seriesShow. This may improve readability based on average image color.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add crew-and-name layout and update series display</div>
                        <div class="details">Introduces a new .crew-and-name layout for crew sections without cast, updates styles for series name display, and removes year from series name in season template. Also adjusts people card sizing and color for localization span.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add user series count and search to series pages</div>
                        <div class="details">Introduces 'userSeriesCount' to controller actions and templates, enabling conditional display of the series search block when the user has fewer than 20 series. Also adds page titles and global data blocks to several series-related templates for improved context and frontend integration.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add series search block for users with <20 series</div>
                        <div class="details">Introduces a search block for series selection on key pages when the user has fewer than 20 series. Updates controller to provide userSeriesCount and tmdbIds, adjusts JS and styles for search functionality, and adds translation for the search placeholder.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Add movie search bar and improve UI handling</div>
                        <div class="details">Introduces a new movie search bar for users with fewer than 20 movies or no movies, including related template and style updates. Refactors JS to support search input and event handling, improves fallback handling for missing posters, and ensures blurred poster and backdrop images are only rendered when available. Also updates French translations for search placeholder.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:youtube-tv') }}→ Improve video page empty state UI and pagination logic</div>
                        <div class="details">Added a styled empty state with image for video pages when no videos are present. Updated pagination logic in VideoController to return an empty string when there are no results or only one page, removing unnecessary HTML output. Enhanced SCSS for better layout and appearance of the empty state.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:menu') }}→ Improve navbar and dialog styles, update footer and menu</div>
                        <div class="details">Enhanced SCSS for navbar layout, dialog appearance, and responsive design. Updated footer image path and improved menu logic for history and avatar display.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor date handling and improve UI in series pages</div>
                        <div class="details">Replaced direct usage of dateService with new helper methods in SeriesController for consistent timezone handling. Updated UserEpisodeRepository to support pagination in episodesToWatch. Improved series.scss with better text alignment and button sizing. Enhanced tmdb.html.twig to conditionally render background and backdrop images for robustness.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:user-outline') }}→ Refactor profile UI and restrict translation settings</div>
                        <div class="details">Refactored Profile.js to use class methods for image preview and file type validation. Updated SCSS for improved layout and preview styling. Translation settings in profile.html.twig are now only visible to user ID 1. UserController now creates default translation settings if missing.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Show TMDB suggestions when user has no movies</div>
                        <div class="details">When a user's movie list is empty, display a header and show TMDB movie suggestions instead of an empty list. Updated MovieController logic, templates, and styles to support this fallback and visually distinguish TMDB cards.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Fix episode quick link click and data attributes</div>
                        <div class="details">Added a check for missing episode number in Season.js to prevent errors on click. Updated season.html.twig to ensure each quick episode div has a data-number attribute and uses consistent variable naming.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>42ᵉ mise à jour : 13/10/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor quick episode links rendering for seasons</div>
                        <div class="details">Moved quick episode link layout logic from Twig to SeriesController for better separation of concerns and maintainability. Added new getQuickLinks() method to generate episode link data and classes, updated season.html.twig to use this data, and adjusted SCSS for new 'first' and 'last' classes. Minor code cleanup and formatting fixes.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:menu') }}→ Navbar AND menus now have blurred background</div>
                        <div class="details">Update navbar and menu styles; add under-navbar element. Refined navbar and menu blur/background styles for improved appearance. Introduced a new .under-navbar element in both SCSS and Twig template for additional UI layering below the navbar.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:menu') }}{{ ux_icon('mdi:tv') }}→ Add watch provider info to pinned series</div>
                        <div class="details">Extended pinned series data to include watch provider logo, name, and link name. Updated repository query, runtime logic, and menu template to display provider information alongside pinned series.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:home') }}→ Add user series status summary to home page</div>
                        <div class="details">Introduces a visual summary of user's series statuses on the home page. Adds backend logic to fetch status counts, updates the controller to pass this data, and styles the new status display section.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:menu') }}{{ ux_icon('mdi:tv') }}→ Add season finale indicator to episode menu</div>
                        <div class="details">Introduces a 'last_episode' flag in UserEpisodeRepository and passes it through to the Twig template. The menu now displays a special icon for episodes that are the last in their season.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add timezone support to series schedules</div>
                        <div class="details">Introduces timezone selection and conversion for series schedules, allowing users to specify and convert air dates and times based on their preferred timezone. Updates controller logic, service methods, form fields, and JavaScript to handle timezone-aware scheduling and display.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refine styles for watch links and user actions</div>
                        <div class="details">Improves the appearance and layout of .watch-links, .watch-link, and related user action elements in series.scss and user.scss. Updates border radii, font sizes, padding, and image dimensions for a more consistent and visually appealing UI.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor localized name and translations assignment</div>
                        <div class="details">Moved assignment of 'translations' and 'localized_name' earlier in the data preparation to ensure correct dependencies. Updated getTvLocalizedName to use Series::getLocalizedName directly and adjusted null check logic for improved clarity.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor SeriesController and update app.scss styles</div>
                        <div class="details">Refactored SeriesController by extracting logic into private helper methods for improved readability and maintainability, and streamlined the handling of series data, votes, schedules, and translations. Updated app.scss to hide horizontal overflow, adjusted transitions, and removed unused media queries and commented code for cleaner styles.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:youtube-tv') }}→ Refactor video modules and improve pagination UI</div>
                        <div class="details">Split Videos.js into Video.js and VideoList.js for better separation of concerns. Updated app.js and importmap.php to use the new modules. Enhanced pagination in VideoController.php to wrap page buttons in a scrollable container, and updated video.scss for improved pagination styling.
                            The active page button is now always centered.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor TV data fetching in SeriesController</div>
                        <div class="details">Extracted TV data retrieval logic into a private getTv() method for improved code reuse and readability. Updated usage in show action and simplified not-found rendering logic.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Update series color handling and styles</div>
                        <div class="details">Commented out color changes for .infos and related elements in app.js, now only updating seriesShow color based on lightness. Added explicit color for series details container in series.scss for improved consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor SeriesController and update not-found view</div>
                        <div class="details">Refactored SeriesController to extract form handling logic into private methods and improved separation of concerns. Updated the not-found series view to use blurred poster backgrounds and display backdrops more consistently. Adjusted Series entity to ensure 'updates' is always an array. Minor style changes to progress bar color in series.scss.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:language-css3') }}{{ ux_icon('mdi:tv') }}→ Update series.scss for improved UI styling</div>
                        <div class="details">Refined background and hover effects for .infos-content, removed unused opacity and margin properties, and added border-radius to navigation elements. These changes enhance visual consistency and improve user experience.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor series navigation and improve UI</div>
                        <div class="details">Moved logic for finding previous and next series into a dedicated method in SeriesController and UserSeriesRepository, improving code clarity and maintainability. Updated the series navigation UI in show.html.twig for better structure and icon consistency. Enhanced series.scss styles for navigation elements and cleaned up unused or commented code.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Refactor movie index JS to external module</div>
                        <div class="details">Moved the movie index page JavaScript from the Twig template to a new ES6 module (MovieIndex.js) for better maintainability and separation of concerns. Updated app.js and importmap.php to register and load the new module. The template now passes required globals via a JSON script block instead of inline JS.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Move 'viewed' indicator into movie card info section</div>
                        <div class="details">Relocates the 'viewed' indicator from the image overlay to the '.infos' section in both the SCSS and Twig template. Updates styles to match the new structure and improve layout consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Add favorite movies page and related features</div>
                        <div class="details">Introduces a new route, controller action, and template to display a user's favorite movies. Adds a repository method to fetch favorite movies, updates the menu to link to the new page, improves favorite icon styling, and adds a translation for 'My favorite movies'.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Improve poster and button SVG styling in SCSS</div>
                        <div class="details">Refines the movie poster display by adjusting flex and image sizing for better aspect ratio and object-fit. Updates user.scss to standardize SVG icon sizes in various button states and adds support for movie-specific button classes.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Refactor movie info layout and styles</div>
                        <div class="details">Reorganized the movie details section by wrapping related elements in a new .infos-content container and updating the associated SCSS for improved structure and maintainability. Adjusted background colors, spacing, and hover effects for better visual clarity. Minor formatting fixes and indentation improvements were also applied to the Twig template.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>41ᵉ mise à jour : 04/10/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:home') }}→ Refactor episode history to use AJAX and new component</div>
                        <div class="details">Replaces DayCountHistory with EpisodeHistory, enabling dynamic loading of episode history via AJAX on the home page. Adds a new controller route for loading episode history, updates repository logic, and introduces a Twig partial for rendering episodes. Updates translations and importmap accordingly.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>40ᵉ mise à jour : 02/10/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add option to include all series in 'What Next' API</div>
                        <div class="details">Updated the SeriesWhatNext API and UserSeriesRepository to allow including series where the user hasn't watched any episodes yet, by adding an $includeFirstEpisode parameter. Adjusted SQL joins accordingly and updated method signatures and usages. This enables the 'What Next' endpoint to show all series, not just those already started by the user.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor series show layout and keyword styling</div>
                        <div class="details">Reworked the series show page to move the backdrop image outside the .infos block and improved the layout for facts, networks, and keywords. Updated keyword block styles for better wrapping and appearance, and added a script to dynamically adjust keyword container width. Adjusted color schemes in SCSS for better contrast and readability.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add user preferred names for guest stars in season view</div>
                        <div class="details">Introduces retrieval of user-specific preferred names for people and displays them alongside guest star names in the season template. Updates SeriesController to fetch and inject preferred names, and modifies the template to show them if available.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor series index to simplify episode queries</div>
                        <div class="details">This page now displays the episodes of the day and for the 7 next days. Simplifies the logic for fetching and displaying episodes of the day and week by consolidating queries and removing unused code. Removes the 'series to start', 'series airing this week', and 'episode history' sections from the index view. Updates SQL queries and variable naming for consistency and future maintainability.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:user-outline') }}→ Improve slug generation for non-Latin names</div>
                        <div class="details">Slug generation now uses preferred names when available and falls back to a default format if the slug is empty, addressing issues with non-Latin characters.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add transitions to series.scss elements</div>
                        <div class="details">The "Add this episode" button ([+]) is now always visible. Introduces smooth transition effects for background color and other properties on specific elements to enhance UI interactivity. Also refactors selector grouping for better maintainability.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:user-outline') }}→ Enhance people block with preferred name and role</div>
                        <div class="details">Updated the people block to display the preferred name and role in the data-title attribute if available, providing more context in the UI.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>39ᵉ mise à jour : 27/09/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ What to watch next query enhanced</div>
                        <div class="details">The SQL query no longer filters on the existence of a last episode viewed, allowing you to see series that have not yet been started.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve season progress tracking and UI feedback</div>
                        <div class="details">Refactored progress calculation to use rounded percentages and updated the API to return both series and season progress. Enhanced UI feedback with new info message styling and hover effects. Fixed a typo in a comment and improved logic for determining the next episode to watch.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:user-outline') }}→ Improve preferred name display and UI styles</div>
                        <div class="details">Refactored the people block to always show both preferred and regular names if available. Updated SCSS to adjust next-button background opacity and highlight names when a preferred name is present. Fixed Season.js to use floored lightness for vote graph styling and added a debug log.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Adjust vote section colors based on background brightness</div>
                        <div class="details">Added logic to Season.js to detect the brightness of the page background and apply a 'dark' class to the vote graph if the background is light. Updated series.scss to style vote elements differently when the 'dark' class is present, improving visibility and accessibility.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Comment out background and border styles in series.scss</div>
                        <div class="details">Temporarily disables background-color and border-radius styles for several components by commenting them out. Also updates the .user-actions selector to apply styles only when h2 is not present.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:menu') }}→ Update navbar and burger styles</div>
                        <div class="details">Adjusted padding for navbar, improved hover selector to exclude burger, and modified burger button dimensions and border radius for better appearance.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Improve album add-photos button layout and responsiveness</div>
                        <div class="details">Moved the add-photos button inside the album-container for better structure. Updated SVG sizing in SCSS to use orientation-based media queries for improved responsiveness in landscape and portrait modes.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}{{ ux_icon('mdi:tv') }}→ Update border radius and opacity in movie and series styles</div>
                        <div class="details">Adjusted border radius for .infos and .backdrop in movie.scss to .5rem for improved visual consistency. Increased opacity and set border radius for elements in series.scss, and reduced blur effect on hover for better UI clarity.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>38ᵉ mise à jour : 25/09/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Update album photo count and animation logic</div>
                        <div class="details">Synchronizes the displayed album photo count with uploads by updating the count in the DOM. Adds logic to initialize album animation when the first two photos are added, and improves image resolution selection for background animation. Refactors AlbumController to redirect after new album creation and updates the album count rendering in the template.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve responsive layout for series posters</div>
                        <div class="details">Enhanced media queries and layout rules for .poster and related elements to better support various screen sizes and orientations. Adjusted widths, heights, and flex properties for improved responsiveness and visual consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve episode grid styling and navigation</div>
                        <div class="details">Refines the border radius and background color for episode and user action elements in series.scss, adds contextual border radius for episode corners, and updates the season template to wrap posters in links and assign corner classes to episode items for better visual distinction. Also adjusts quick-episodes grid width calculation for improved layout.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>37ᵉ mise à jour : 21/09/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:bug-outline') }}→ Update TMDB API requests to use explicit headers</div>
                        <div class="details">Replaces 'auth_bearer' option with explicit 'Authorization' and 'accept' headers in TMDBService API requests. Ensures proper authentication and content negotiation for TMDB API calls.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Fix seriesAround lookup to use userSeries ID</div>
                        <div class="details">Replaces usage of series ID with userSeries ID when fetching surrounding series. This ensures the correct context is used for user-specific series navigation.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Add max-height and scroll to preview images</div>
                        <div class="details">Introduced a max-height and vertical scrolling for the .preview-image-files class to improve usability when displaying large sets of images.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }}→ Enhance map popups with location and improved styling</div>
                        <div class="details">Added location details to map popups and formatted POI descriptions with line breaks. Updated map and series SCSS to improve popup content styling, including scrollable descriptions and highlighted location sections.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add season poster display and hover effect</div>
                        <div class="details">Introduces a poster image for seasons in the template if available, and updates the SCSS to style the poster with a hover effect that enlarges it. Improves visual presentation and interactivity for season details.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>36ᵉ mise à jour : 18/09/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:update') }}→ Add updates for 35th and 36th releases in updates page</div>
                        <div class="details">Added detailed changelogs for the 35th and 36th site updates to updates.html.twig, including new features, refactors, bug fixes, UI improvements, and deployment instructions. These entries provide users and developers with a comprehensive summary of recent changes and enhancements across the application.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:administrator') }}{{ ux_icon('mdi:tools') }}→ Add admin tools page with find/zip command utility</div>
                        <div class="details">Introduces a new admin tools section accessible via '/tools', including a UI and JavaScript utility for calculating minutes since a given date and generating a find/zip shell command. Updates styles and translations to support the new tools page and integrates it into the admin navigation.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Update remaining episodes display logic in series card</div>
                        <div class="details">Changed the display for remaining episodes in the series card to show 'Last episode' when only one episode remains, instead of '1 remaining episode'. This improves clarity for users viewing series progress.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Optimize network lookup and update logic in SeriesController</div>
                        <div class="details">Replaces repeated database queries with a single batch fetch for networks, improving performance. Removes unnecessary null coalescing for TMDB fields, assuming their presence, and updates how network entities are created and updated.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:server-network') }}→ Update deployment instructions for asset handling</div>
                        <div class="details">Added alternative steps for zipping and unzipping assets and public files, including instructions for handling recent file modifications and separating assets. These changes provide more flexible deployment options depending on project needs.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}{{ ux_icon('mdi:filmstrip-box') }}→ Add ToolTips initialization to WatchLinkCrud</div>
                        <div class="details">Imported the ToolTips module and initialized it within the WatchLinkCrud class constructor to enable tooltip functionality.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Fix episode selector and fetch ID in nowEpisode</div>
                        <div class="details">Corrects the selector to use 'data-ue-id' and updates the fetch call to use 'episodeId' instead of 'id' in the nowEpisode method. Ensures proper episode identification when marking as watched.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}{{ ux_icon('mdi:filmstrip-box') }}→ Refactor watch link CRUD and keyword translation APIs</div>
                        <div class="details">Extracted watch link CRUD logic into a reusable WatchLinkCrud JS class and unified its usage in both movie and series views. Moved keyword translation save API to a dedicated controller and updated frontend to use the new endpoint. Updated styles and templates to support new form layouts and popover handling. Cleaned up MovieController by delegating provider logic to WatchLink API and removed legacy keyword translation code.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }}→ Add Mapbox SearchBox and related dependencies</div>
                        <div class="details">Integrated MapboxSearchBox into the map for enhanced search functionality, including reverse geocoding and marker support. Updated importmap.php to include required Mapbox and UI dependencies.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Enhance movie image display and language filtering</div>
                        <div class="details">Improves the movie page by adding dynamic filtering of posters, backdrops, and logos by language, updating image counts, and adjusting text color based on backdrop brightness. Refactors Movie.js for better modularity, updates MovieController to provide image counts, and enhances the template and styles for conditional image section rendering and improved UI responsiveness.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Add multi-language movie images display</div>
                        <div class="details">Introduces support for displaying posters, backdrops, and logos filtered by language on the movie page. Adds a language selector, updates MovieController to fetch and process images from TMDB, and enhances frontend JS and SCSS for diaporama and tooltips. Also updates French translations for new image-related terms.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>35ᵉ mise à jour : 15/09/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Fix photo count translation and pluralization</div>
                        <div class="details">Improves photo count display by correctly handling singular and plural forms in album listings. Adds missing 'photo' and 'photos' keys to French translations for proper localization.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}{{ ux_icon('mdi:filmstrip-box') }}→ Use blurred poster images for backgrounds</div>
                        <div class="details">Updated controllers and templates to use blurred poster images for movie and series backgrounds. Refactored ImageService to support media type in blurPoster method and fixed file path naming. Adjusted styles and removed some externals block includes for consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:photo-library') }}→ Fix srcset attribute handling in Diaporama</div>
                        <div class="details">Ensure 'srcset' is only set when a value exists to prevent empty or undefined attributes on image elements. This improves robustness and avoids potential rendering issues.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}{{ ux_icon('mdi:filmstrip-box') }}→ Refactor overview forms to use popover and improve UX</div>
                        <div class="details">Converted overview, delete-overview, and localized-name forms to use the popover attribute for better modal handling. Updated JavaScript logic to support popover API, improved form show/hide logic, and refactored source display code. Adjusted styles for popover forms and updated button markup in Twig templates for accessibility and consistency. Fixed API response keys and repository method signature for consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Add Photos page with diaporama support</div>
                        <div class="details">Introduces Photos.js to handle the album photos page and initializes Diaporama for image viewing. Updates Diaporama.js to support srcset for responsive images and refactors image navigation logic. Adjusts album.scss for improved styling, updates importmap.php for new module, and refines photos.html.twig for correct image paths and SVG controls.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add blurred poster backgrounds for series and seasons</div>
                        <div class="details">Introduces blurred poster image generation in ImageService and updates SeriesController to provide blurred poster paths for series and seasons. Twig templates now use blurred poster images for background display, improving visual aesthetics and readability.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:language-css3') }}→ Refactor color variables and alpha usage in stylesheets</div>
                        <div class="details">Replaces custom alpha color variables with direct use of the CSS hsla() function and the 'from' syntax for dynamic color manipulation across all SCSS files. Simplifies color variable definitions in _variables.scss and updates all references to use hsla(from ...) for alpha transparency, improving maintainability and consistency of color usage throughout the styles.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:language-css3') }}→ Refactor and update styles for overlays and votes</div>
                        <div class="details">Replaces several alpha color variables with hsla() for improved color control, updates border-radius for .next-button, and removes or comments out unused background-color and text-shadow styles. Also sets .backgroundImageOverlay to use overflow: hidden for better layout handling.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}{{ ux_icon('mdi:filmstrip-box') }}→ Refactor overview sections and JS event handling</div>
                        <div class="details">Moved overview section headers inside their respective containers in movie and series templates for better structure. Updated TranslationsForms.js to use dedicated editOverview and deleteOverview methods for event handling, and improved DOM selection logic. Adjusted SCSS to ensure overview containers are hidden when empty and moved header styles inside .overviews for consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:home') }}→ Update home route and redirects to app_home_index</div>
                        <div class="details">Changed all redirects and route definitions from 'app_home' to 'app_home_index' across multiple controllers for consistency. Also updated the HomeController route to use '/' as the base path and '/home/{_locale}/' for localized home pages.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:language-css3') }}→ Update navbar and menu styles with blur effect</div>
                        <div class="details">Applied backdrop-filter blur and semi-transparent background to .navbar and menu elements for improved visual appearance. Adjusted navbar item alignment when .multi-search is absent and commented out some hover styles for clarity.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:user-outline') }}→ Fix error when images container is missing</div>
                        <div class="details">Added optional chaining to prevent errors if the .images element is not present when initializing the diaporama in PeopleShow.js.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:language-css3') }}→ Refactor theme and color variables for CSS and JS</div>
                        <div class="details">Updated theme management in Menu.js to use data-theme on :root and removed legacy body class logic. Refactored SCSS variables to use light-dark() and consolidated color variables, replacing many --grey and --ardoise references with new variables. Updated all stylesheets to use the new color variables and gradients, improving consistency and maintainability for light/dark themes.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>34ᵉ mise à jour : 11/09/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }} → Add consistent padding to map fitBounds calls</div>
                        <div class="details">Standardizes the padding option to 45 for all map.fitBounds calls to improve map display consistency. Also fixes a minor whitespace typo in property assignment.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor series info layout and styling on season page</div>
                        <div class="details">Reorganized the structure of series and season info sections by introducing a new .infos-content wrapper in templates and updating related CSS for better layout control. Improved color contrast logic in JS for season pages and adjusted various style details for consistency and readability.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Redesign album sidebar and improve photo sorting</div>
                        <div class="details">Refactored sidebar and toggler styles in album.scss for a more modern UI and moved sidebar markup to photos.html.twig with improved toggler logic. Updated AlbumController to sort photos by date descending and optimized image path extraction. Changed page title to 'Photos' for clarity.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }} → Refactor marker handling and album photo logic</div>
                        <div class="details">Commented out debug console logs in Map.js and improved marker handling by adding a 'photo-marker' class to photo markers. The adjustBounds method now accepts a marker type for more precise bounds adjustment. In AlbumShow.js, removed redundant addPhotosButton logic and updated photo marker addition to use the new marker type, also ensuring image paths are tracked.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Add photos overview page and update album features</div>
                        <div class="details">Introduces a new 'Photos' overview page with dedicated route, controller logic, and template. Updates album controller to refactor album creation, add album date ranges, and improve daily album grouping. Enhances styles for the new page, updates navigation menus, and adds French translations for new labels.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Add thumbnail image generation and update paths</div>
                        <div class="details">Introduces thumbnail image generation at 320px width in both AddMobileResPhotoCommand and ImageService. Updates logic to check for and create thumbnails instead of mobile resolution images, and ensures necessary directories are created. Also exposes checkForPaths as public in ImageService.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor season header layout and progress bar</div>
                        <div class="details">Moved the season progress bar outside the poster element in the season template and updated its styling in SCSS. Improved image selection logic for color extraction in app.js to better support season pages. Adjusted header and overview styles for better visual consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }} → Improve map marker filtering and padding</div>
                        <div class="details">Increased map fitBounds padding from 15 to 45 for better visibility. Updated marker filtering logic to exclude markers with the 'poi-marker' class from being hidden or shown, and fixed the 'All series' option value in the series select dropdown.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }} → Limit POI bounds calculation to first 5 items</div>
                        <div class="details">Updated AdminController to calculate map bounds using only the first 5 points of interest, improving performance and relevance for large datasets. Also fixed Twig template to remove extra curly braces around keyboard icon rendering.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add support for schedule overrides in series view</div>
                        <div class="details">Introduces handling and display of schedule overrides for series seasons. The controller now sets a 'final_air_date' when an override is present, and the UI shows an override icon and uses the override date for season air date display. Also includes minor code style fixes for array formatting.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }} → Move map logic from template JS to Map.js</div>
                        <div class="details">Refactored map interaction logic from inline JavaScript in index.html.twig to the Map.js module for better maintainability and separation of concerns. Updated template to remove redundant scripts and ensure map initialization and controls are handled in Map.js. Also added missing map container IDs for consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }} → Refactor POI loading and map marker rendering</div>
                        <div class="details">Points of Interest (POIs) are now fetched via a dedicated API endpoint and rendered dynamically on the map, removing POI data from series and season endpoints. Updated Map.js to fetch and display POIs, added src/Api/PointOfInterest.php for POI API, and adjusted templates and styles for improved marker appearance and separation of POI logic.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }} → Enhance POI markers and popup behavior on map</div>
                        <div class="details">Custom POI markers now use images and improved styling via .poi-marker class. Popups for locations, POIs, and photos now close on map move for better UX.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:update') }}→ Update user updates page and style SVG icons</div>
                        <div class="details">Added a new update section to the user updates page with detailed changelog entries and standardized arrow spacing in change descriptions. Updated SCSS to set consistent height and width for SVG icons within update entries for improved visual alignment.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>33ᵉ mise à jour : 07/09/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Add user avatar display to album cards</div>
                        <div class="details">Add user avatar display to album cards</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Update album image paths and French translations</div>
                        <div class="details">Changed image paths in album templates to use 'mobile' versions and updated srcset attributes to include 360w images. Reordered and added several album-related translation keys in messages.fr.yaml for improved clarity and consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Add mobile resolution photo support</div>
                        <div class="details">Introduces AddMobileResPhotoCommand to generate mobile resolution (max 640px) images for photos. Updates ImageService to handle mobile resolution creation and exposes resizeWebpImage as public. Adds photoAll() method to PhotoRepository for retrieving all photos.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Refactor cast and recommendations layout structure</div>
                        <div class="details">Removed unnecessary 'content' wrapper from cast, crew, and recommendations sections in the movie details template. Updated SCSS to match new structure and improved flexbox handling for better layout consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tooltip-text') }}→ Optimize tooltip style updates and cleanup code</div>
                        <div class="details">Added caching for tooltip style to prevent redundant DOM updates and improve performance. Removed commented and unused code for better readability.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Refactor photo storage to support per-user album paths</div>
                        <div class="details">Photo image paths now include the user ID in the directory structure for better separation and organization. Updated backend logic, entity, repository, and service methods to handle per-user paths and prevent duplicate photo uploads. Frontend and templates were adjusted to use the new path format for displaying images and handling uploads, and map bounds adjustment was added for improved UX.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Refactor album and photo entities to support many-to-many</div>
                        <div class="details">Updated Album and Photo entities to use a many-to-many relationship, allowing photos to belong to multiple albums. Added a 'userCreated' flag to albums to distinguish between user-created and auto-generated albums. Refactored controller logic and templates to accommodate these changes, including album creation, display, and navigation. Adjusted repository and styling as needed.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:comment-flash') }}→ Fix event listener binding in FlashMessage closeAll</div>
                        <div class="details">Replaces usage of e.currentTarget with explicit selection of the .flash-messages element for adding and removing click event listeners in the closeAll method. This ensures the correct element is targeted regardless of the event context.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve series episode display and styling</div>
                        <div class="details">Added support for showing first air date from schedule if not present in series. Updated SCSS to style .from-schedule spans. Refactored episode display logic to only show last/next episode sections if data is available, improving conditional rendering and preventing empty blocks.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:search') }}→ Refactor search endpoints and menu search logic</div>
                        <div class="details">Unified and renamed search API endpoints in controllers for movies, series, and people. Refactored Menu.js to use new endpoints and improved search result handling, including multi-search and menu closing logic. Updated SCSS for search input and results styling, and adjusted menu template to include &lt;ul&gt; elements for search results. Also fixed SQL alias in UserSeriesRepository.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>32ᵉ mise à jour : 03/09/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }}→ Increase map popup width and improve styling</div>
                        <div class="details">Set the maximum width of map popups to 24rem in both JavaScript and template code for better content display. Updated SCSS to match the new width and added border radius to popup titles for improved appearance.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:home') }}→ Add dynamic provider series loading on home page</div>
                        <div class="details">Implemented AJAX-based loading of series by provider on the home page. Added a new endpoint and controller method for fetching provider-specific series, updated JavaScript to handle provider selection and update the UI dynamically, and refactored Twig templates to support the new functionality. Also standardized route and path names for home page references across templates.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:menu') }}→ Simplify menu by merging videos and albums sections</div>
                        <div class="details">Combined the videos and albums menu items into a single section with a unified icon. Removed individual search inputs and search result containers for both videos and albums to streamline the navigation menu.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Refactor album photo layout and container structure</div>
                        <div class="details">Reorganized the album photo section by introducing an 'album-container' wrapper and moving background elements for improved layout control. Updated SCSS to support the new structure, enhance grid/list switching, and adjust background overlays. The number badge is now commented out in the template, and the edit button remains. These changes improve maintainability and visual consistency for album photo displays.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>31ᵉ mise à jour : 01/09/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:administrator') }}→ Improve new episodes label and admin styles</div>
                        <div class="details">Updated the label for new episodes in SeriesUpdates.php to correctly pluralize 'episode' based on count. Enhanced the admin.scss styles for the new-episodes field by adding flex layout, centering, and spacing, and commented out fixed width for usernames.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:menu') }}→ Update menu layout and reorganize admin/contact links</div>
                        <div class="details">Moved the 'Admin' section separator above the admin dashboard link and added a new separator and contact link for questions or comments. Also commented out the 'Home' and 'My movies' links in the main menu and removed duplicate contact section.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve network handling in SeriesController</div>
                        <div class="details">Refactored network processing to iterate over $tv['networks'] instead of $ids, improved variable naming for clarity, and added error handling for missing TMDB network details. Also updated logic to ensure network updates and flash messages are more robust and informative.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:youtube-tv') }}→ Update video grid layout and adjust back icons</div>
                        <div class="details">Refactored the video grid from flex to CSS grid for improved responsiveness, adjusting column counts for different screen sizes. Also commented out the 'arrow-back' icons in the video show template to simplify the back button appearance.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }} {{ ux_icon('mdi:map-outline') }}→ Add cooperativeGesturesOption to Map initialization</div>
                        <div class="details">Refactored Map class to accept a cooperativeGesturesOption parameter, allowing control over the map's gesture behavior. Updated all Map instantiations to pass the appropriate option based on context. Also fixed a minor indentation issue in a comment block in Season.js.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }} {{ ux_icon('mdi:map-outline') }}→ Refactor photo marker method and handle missing captions</div>
                        <div class="details">Renamed addMarker to addPhotoMarker in Map.js and updated all references for clarity. Improved marker popup HTML to conditionally display the photo caption only if present. In AlbumController.php, set the photo caption to null when not available to prevent undefined values.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }} {{ ux_icon('mdi:tv') }}→ Refactor translation tools to shared TranslationsForms module</div>
                        <div class="details">Extracted and unified translation-related UI logic for movies and series into a new TranslationsForms JavaScript module. Updated Movie.js and Show.js to use this shared component, simplifying their code. Updated styles to use new .localization-* classes and moved translation tool styles to a dedicated translations.scss file. Adjusted markup and selectors for consistency, and updated importmap.php to register the new module.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>30ᵉ mise à jour : 28/08/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Refactor image file handling and remove debug dumps</div>
                        <div class="details">Commented out debug dump statements in AlbumController and ImageService. Updated ImageService to consistently use the basename with extension when moving uploaded files, improving file naming consistency.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Refactor movie card to make entire card clickable</div>
                        <div class="details">Moved the anchor tag to wrap the entire movie card, making the whole card clickable instead of just a part of it. Added a hover outline effect to the card for better visual feedback.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Improve movie page layout and update date formats</div>
                        <div class="details">Refactored movie index layout to use CSS grid for better responsiveness and adjusted breakpoints from 50rem to 52rem across multiple SCSS files. Updated movie card date displays to use 'relative_medium' format for improved readability. Also added movie count to the main page heading and made minor style enhancements for filter buttons and responsive design.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Attempt to resolve an error that does not occur in development mode</div>
                        <div class="details">Show an error flash message when a network is not found in SeriesController, including the network name and ID. Added the corresponding French translation for 'network.not_found' in messages.fr.yaml.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }}→ Rename last locations route and template</div>
                        <div class="details">Renamed the route and controller method from 'last-locations' to 'last', and updated the template from 'last-creations.html.twig' to 'last.html.twig'. Also fixed a typo in the icon name from 'image-mutiple' to 'image-multiple' in the template.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Refactor user action controls and update styles</div>
                        <div class="details">Consolidated 'sizes', 'aspect-ratios', and 'edit-episode-infos' into a unified 'user-actions' class in both HTML and JS selectors. Updated SCSS to style the new structure, removed redundant classes, and improved selector specificity. Also added a null check for the filming locations button event listener.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:bug-outline') }}→ The killer death bug who kills… javascript!</div>
                        <div class="details">It's like desactivate javascript</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add override flag to alternate schedule handling</div>
                        <div class="details">Introduces an 'override' property to alternate schedule arrays in SeriesController and updates the Twig template to display alternate schedules only when the override flag is set. This change improves the accuracy of alternate schedule presentation and ensures only relevant schedules are shown.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:emoticon-cow') }}→ Switch all icons to Material Design Icons (mdi)</div>
                        <div class="details">Replaced all FontAwesome, Iconoir, and other icon references with Material Design Icons (mdi) equivalents across JavaScript and Twig template files. This unifies the icon set used throughout the application for consistency and easier maintenance.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Improve album naming for recent days</div>
                        <div class="details">Album names for photos taken within the last week now use the day of the week instead of a relative date format. This enhances clarity for users viewing recent albums.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>29ᵉ mise à jour : 25/08/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add sorting order to seriesToStart query</div>
                        <div class="details">Introduces a separate 'order' variable set to 'DESC' and updates the call to seriesToStart to accept both sort field and order. This prepares the code for future enhancements, such as allowing users to choose the sorting order.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }}→ Refine map header styles and icon sizes</div>
                        <div class="details">Adjusted the series map header styles for improved layout and readability, including font sizes and spacing. Reduced the icon sizes in the numbers section from 32px to 24px for better visual balance.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:language-css3') }}→ Update menu icons to use mdi icon set</div>
                        <div class="details">Replaced various icon references in the navigation menu with their corresponding Material Design Icons (mdi) equivalents. This change standardizes the icon set used in the menu for a more consistent appearance.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:map-outline') }}→ Leaflet popup description background color corrected</div>
                        <div class="details"></div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Refactor album grid cell class logic and UI checks</div>
                        <div class="details">Moved the album grid cell class calculation to a dedicated getCellClasses() method in AlbumController for better maintainability and reuse. Updated AlbumShow.js to add null checks for DOM elements and event listeners, improving robustness. The album edit button in the template is now only shown for albums with a valid ID. Minor CSS adjustment to grid row sizing.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>28ᵉ mise à jour : 24/08/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Improve album grid layout and photo styling</div>
                        <div class="details">Enhanced the album grid by adding new CSS classes for various grid spans and adjusted the logic in AlbumController to better handle cell class assignment for different photo counts. Updated photo and SVG styling for better appearance and responsiveness. Also removed or commented out debug dump statements in AlbumController and ImageService for cleaner logs.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Refactor album photo grid and add photo button UI</div>
                        <div class="details">Updated the album photo grid to use a 4-column layout with improved grid spanning logic for photos, both in the backend (cellClasses) and frontend (JS and Twig). The add photo button is now positioned absolutely and always visible for albums, and a number badge is shown on each photo. Improved handling for albums with no photos, and removed unused/obsolete CSS and logic.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Add daily photo albums and improve album handling</div>
                        <div class="details">Introduces support for grouping photos by day as virtual albums, adds a new route and controller logic for date-based albums, and updates templates to handle both standard and date-based albums. Refactors album and photo array conversion, improves album form and photo addition UI, and fixes minor issues with date handling and EXIF extraction. Also updates translations and album entity to support new features.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Add EXIF extraction and display photo metadata in album</div>
                        <div class="details">This update extracts EXIF data (date, latitude, longitude) from uploaded photos and stores them in the database. The frontend now displays photo metadata (date, created/updated at) in the album view, and the UI for adding photos and empty album states has been improved. Various code and style adjustments were made to support these features.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Refactor album background animation and layout</div>
                        <div class="details">Replaces the single background image with a dual-layer background for smoother transitions in the album view. Updates AlbumShow.js to handle two background images and animate transitions. Refactors related SCSS for new background structure and improves layout responsiveness. Updates the album show template to use the new background markup.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Order filming locations by season and episode</div>
                        <div class="details">Added ORDER BY clause to SQL query in FilmingLocationRepository to sort results by season_number and episode_number. This ensures that filming locations are returned in chronological order.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add sorting options to 'series to start' page</div>
                        <div class="details">Introduces UI controls and backend support for sorting the 'series to start' list by first air date or date added, in ascending or descending order. Updates styles, controller, repository, and templates to handle sorting, and adds relevant translation strings.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>27ᵉ mise à jour : 21/08/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve network handling and add French translations</div>
                        <div class="details">Refactored network processing in SeriesController to add or update networks as needed, with flash messages for added or updated networks. Added French translations for network addition and update notifications.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:bug-outline') }}→ Display detailed exception info on error page</div>
                        <div class="details">Enhanced the error page to show exception message, code, and file/line details if available. Updated ErrorController to extract exception data from the request and pass it to the template. Added styling for exception details in app.scss.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Update air date for user episodes if missing</div>
                        <div class="details">Adds logic to set and save the air date for user episodes when it is missing but available from the episode data.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:search') }}→ Refactor multi-search click handling and result clearing</div>
                        <div class="details">Improves event handling for multi-search results by refining click logic and focusing behavior. Also updates result clearing to remove individual list items instead of resetting innerHTML, and adds navigation on result selection.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve series card ranking display and vote formatting</div>
                        <div class="details">Refactors the series card ranking UI by moving the ranking index and average vote to overlay badges on the poster, with updated styles for better visibility. The average vote is now rounded to two decimal places in the controller for consistent formatting.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Refactor album page layout and background structure</div>
                        <div class="details">Updated album.scss and show.html.twig to improve the album page layout. Moved the background image markup inside the .protection div, adjusted padding and height calculations, and enhanced photo hover effects for better UI consistency.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>26ᵉ mise à jour : 20/08/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Add series ranking by vote feature</div>
                        <div class="details">Introduces a new page to display user's series ranked by average episode vote. Adds a new controller action, repository method, menu entry, translations, and related templates and styles for the ranking view.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Fixes a bug related to episode voting on the series page</div>
                        <div class="details">Episode views are represented by a record in the ‘user_episode’ table. For an episode, there are as many records as there are views. Voting is only present on the first view record. Bug fixing: eliminate extra view records.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>25ᵉ mise à jour : 18/08/2025</h2>
                <ul>
                    <li>
                        <div class="change">{{ ux_icon('mdi:language-css3') }}→ Update media queries to use container query syntax</div>
                        <div class="details">Replaced max-width media queries with container query syntax (width <= 64rem and width <= 50rem) in series.scss. Also adjusted .series-show and .header styles for improved responsiveness on smaller screens.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:filmstrip-box') }}→ Update responsive styles for movie pages</div>
                        <div class="details">Commented out the media query for widths above 576px and added a new media query for widths up to 50rem. The new styles adjust layout, poster sizing, and border radius for improved responsiveness on smaller screens.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Refactor album page layout and improve responsiveness</div>
                        <div class="details">Reorganized the album page structure in show.html.twig for better separation of controls and improved accessibility. Updated SCSS styles for .album-page, layout buttons, and responsive breakpoints to enhance mobile and desktop experiences. Moved map height CSS variables to :root in map.scss for consistency. Adjusted layout controls to use icon buttons and improved their styling and grouping.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:tv') }}→ Improve edit button visibility on episode hover</div>
                        <div class="details">Changed the .edit button to use opacity transitions instead of display toggling for smoother appearance on hover. Updated hover logic to show the edit button only when not editing, and removed commented-out legacy code.</div>
                    </li>
                    <li>
                        <div class="change">{{ ux_icon('mdi:image-album') }}→ Refactor album view with animated background and layout logic</div>
                        <div class="details">Moved album layout switching and settings persistence from inline Twig script to AlbumShow.js, centralizing logic and improving maintainability. Added animated background image for grid layout, updated SCSS for new layout structure and responsive design, and restructured the album page HTML to use semantic elements and support the new background/protection overlays. Minor string formatting improvements and animation logic refactor in AlbumShow.js.</div>
                    </li>
                    <li>
                        <div class="change">→ Refactor user episode addition to prevent duplicates</div>
                        <div class="details">Updated the logic in addSeasonToUser and related methods to check for existing user episodes before adding new ones, preventing duplicate entries. Modified method signatures to pass userEpisodes where needed and improved episode addition efficiency by batching database flushes.</div>
                    </li>
                    <li>
                        <div class="change">→ Refactor map UI into reusable map-container component</div>
                        <div class="details">Introduced a new map.scss stylesheet and a reusable _map_container.html.twig partial to unify and modernize the map UI across album, series, and map pages. Updated all relevant templates to use the new component, removed redundant map-related styles from album.scss and series.scss, and improved toggler and thumbnail logic. This change centralizes map styling and markup, making future maintenance and enhancements easier.</div>
                    </li>
                    <li>
                        <div class="change">→ Set navbar opacity to always be 1</div>
                        <div class="details">Replaces dynamic opacity calculation with a constant value of 1 for the navbar. This change disables scroll-based opacity adjustments, likely for improved visibility or debugging.</div>
                    </li>
                    <li>
                        <div class="change">→ Add next/last episode tabs to series season view</div>
                        <div class="details">Enhanced the series season view with new tabs for last and next episodes, including episode details and images. Updated JavaScript and SCSS for tab behavior and styling. Controller now provides episode data and handles new episodes for user tracking. Added relevant translations for new UI elements.</div>
                    </li>
                    <li>
                        <div class="change">→ Add user vote graphs to series seasons</div>
                        <div class="details">Introduces a reusable Twig partial for displaying user vote graphs per season and integrates it into the series show and season views. Updates JavaScript and SCSS to support interactive display of user votes, and extends the SeriesController to provide user vote data to templates.</div>
                    </li>
                    <li>
                        <div class="change">→ Refactor album grid layout and improve album card UI</div>
                        <div class="details">Switched album listing from flex to CSS grid for better responsiveness and control. Adjusted album card styles: added aspect ratio, made info overlay appear on hover with smooth transition, and updated border radius. Improved responsive breakpoints for different screen sizes.</div>
                    </li>
                    <li>
                        <div class="change">→ Add filming locations map feature for series on season page</div>
                        <div class="details">Introduces a new API endpoint and frontend integration to display filming locations for a series. Adds a button to the season page that fetches and displays a map with filming locations using data from the backend. Updates styles and translations to support the new feature.</div>
                    </li>
                    <li>
                        <div class="change">→ Add progress bar for bulk image addition in series</div>
                        <div class="details">Enhanced the 'Add all available images' dialog for series to display poster and backdrop counts and a progress bar when adding many images. Updated the JavaScript to handle bulk image uploads with progress feedback, adjusted the controller to support per-image addition, improved dialog layout and styles, and updated French translations for new UI elements.</div>
                    </li>
                    <li>
                        <div class="change">→ Refactor movie viewed status and display view dates</div>
                        <div class="details">Changed UserMovie 'viewed' property from boolean to integer to track view count. Updated MovieController to store formatted view dates and ensure uniqueness. Modified frontend and styles to display multiple view dates and improved UI for marking as viewed.</div>
                    </li>
                    <li>
                        <div class="change">→ Enhance network details with homepage and headquarters</div>
                        <div class="details">Adds fetching and display of network headquarters and homepage from TMDB, updates SeriesController to refresh network data if older than 30 days, and improves UI to show external link icon for network homepage in series details.</div>
                    </li>
                    <li>
                        <div class="change">→ Add force update option to series admin panel</div>
                        <div class="details">Introduces a 'Force update' checkbox to the admin series updates UI, allowing updates to bypass the usual time check. Updates the backend to handle the force flag, improves user episode update logic, and enhances styling for new episodes and user badges. Also adds French translation for 'Force update'.</div>
                    </li>
                    <li>
                        <div class="change">→ Add user-specific new episode tracking in series updates</div>
                        <div class="details">Introduces logic to track and display new episode count added for each user in series updates. Updates the admin JS and SCSS to handle and style the new 'new_episodes' field, and adds a helper to render user info in HTML for update logs.</div>
                    </li>
                    <li>
                        <div class="change">→ Add image saving for series poster and backdrop updates</div>
                        <div class="details">Integrates ImageConfiguration and ImageService to save updated poster and backdrop images when their paths change during series updates. Ensures images are stored using the correct URLs and sizes.</div>
                    </li>
                    <li>
                        <div class="change">→ Add navigation and improve video category handling</div>
                        <div class="details">Introduces 'Back to my videos' and 'Back to my videos by category' navigation buttons on the video show page, with corresponding styles and translations. Refactors category sorting logic into a private method in VideoController and ensures category/page context is preserved in navigation. Enhances JS to manage video category selection and option disabling more robustly.</div>
                    </li>
                    <li>
                        <div class="change">→ Update Mapbox GL JS and related dependencies to v3.14.0</div>
                        <div class="details">Upgraded Mapbox GL JS and Mapbox GL Geocoder to v3.14.0 and v5.1.0 respectively across JS, PHP importmap, and Twig templates. Adjusted map initialization to support new features (pitch, bearing, show3dObjects) and improved SCSS selectors for pagination. Updated other JS and PHP dependencies to latest versions for compatibility.</div>
                    </li>
                    <li>
                        <div class="change">→ Enhance 'What to Watch Next' with TMDB similar series</div>
                        <div class="details">The 'What to Watch Next' feature now fetches similar series from TMDB if fewer than 20 recommendations are available, using the current series' TMDB ID and language. Frontend and backend were updated to pass and handle these parameters, and the UI was improved for better display. TMDBService now supports language selection for similar series queries.</div>
                    </li>
                    <li>
                        <div class="change">→ Add 'What to watch next' feature for series</div>
                        <div class="details">Introduces a new 'What to watch next?' button on the season page, allowing users to fetch and display recommended series cards via a new API endpoint. Includes supporting backend controller, frontend JS logic, styles, and translation updates.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>24ᵉ mise à jour : 06/08/2025</h2>
                <ul>
                    <li>
                        <div class="change">→ Refactor movie details layout and improve responsiveness</div>
                        <div class="details">Reworked the movie details section by replacing the 'two-parts' wrapper with separate 'left' and 'right' columns for better structure and maintainability. Updated SCSS to improve responsive behavior, especially for mobile and tablet breakpoints, and adjusted HTML templates to match the new layout. This enhances
                            readability and flexibility for future UI changes.
                        </div>
                    </li>
                    <li>
                        <div class="change">→ Improve mobile navbar menu UX and styling</div>
                        <div class="details">Enhanced the mobile navigation menu by adding toggling functionality via JavaScript and updating SCSS styles for better layout, overflow handling, and responsive design. The changes improve accessibility, menu appearance, and user experience on smaller screens.</div>
                    </li>
                    <li>
                        <div class="change">→ Improve responsive styles and add navbar toggler</div>
                        <div class="details">Updated media queries to use rem units and adjusted breakpoints for better responsiveness across album, app, and video SCSS files. Refactored navigation bar to include a toggler button and improved mobile layout handling in _menu.html.twig. Enhanced UI consistency and usability on smaller screens.</div>
                    </li>
                    <li>
                        <div class="change">→ Add collapsible advanced search UI</div>
                        <div class="details">Introduces a folded state for the advanced search form, allowing users to collapse and expand the search interface using magnify and close icons. Updates related SCSS for styling the new UI states and adjusts font size for improved readability.</div>
                    </li>
                    <li>
                        <div class="change">→ Improve admin series updates summary styling</div>
                        <div class="details">Adjusted aspect-ratio spacing for poster and backdrop images. Enhanced layout and overflow handling for series updates summary, including detail fields and name widths for better readability and usability.</div>
                    </li>
                    <li>
                        <div class="change">→ Add photo editing dialog and backend support</div>
                        <div class="details">Introduces a new side panel dialog for editing photo metadata (caption, date, coordinates) in albums, with corresponding JavaScript logic for form handling and UI updates. Adds a backend endpoint in AlbumController for processing photo edits and updates the Album entity to use proper getters/setters for date range
                            properties. Refactors CSS and template structure to support the new dialog and improves maintainability by extracting form logic into dedicated methods.
                        </div>
                    </li>
                    <li>
                        <div class="change">→ Add album layout switcher and prev/next navigation</div>
                        <div class="details">Introduces grid/list layout switching for album photos with user settings persistence, and adds previous/next album navigation with improved styling. Updates AlbumController, AlbumRepository, and templates to support these features, and enhances translations and SCSS for the new UI elements.</div>
                    </li>
                    <li>
                        <div class="change">→ Add localized date range string to albums</div>
                        <div class="details">Introduces a dateRangeString property to the Album entity, generated and localized in AlbumController using TranslatorInterface. Updates the album index template to display the formatted date range for each album.</div>
                    </li>
                    <li>
                        <div class="change">→ Enhance diaporama with srcset support and UI improvements</div>
                        <div class="details">Added srcset handling in the Diaporama JavaScript to support responsive images. Updated SCSS for better diaporama layout, including full viewport sizing and improved thumbnail visibility on hover. Modified Album entity constructor for explicit initialization. Extended photo srcset in album template to include
                            original image size.
                        </div>
                    </li>
                    <li>
                        <div class="change">→ Improve album photo upload and display handling</div>
                        <div class="details">Added support for dynamic photo addition with responsive srcset paths after upload, and improved handling of photos without geolocation in the map. Updated AlbumController to return image paths on upload and pass srcset paths to the frontend. Adjusted album page and photo grid styles for better layout and
                            scrolling.
                        </div>
                    </li>
                    <li>
                        <div class="change">→ Add map style toggler and thumbnail list to album view</div>
                        <div class="details">Introduces a floating toggler button to show or hide the map style thumbnail list in the album view. Updates styles for positioning and transitions, adds a script for toggling the list, and enhances thumbnail markup with a data-title attribute.</div>
                    </li>
                    <li>
                        <div class="change">→ Add 'My albums' button to album page</div>
                        <div class="details">Introduced a new 'My albums' button in the album page tools section and added corresponding styles in album.scss. Also adjusted map controller height in the series-map section.</div>
                    </li>
                    <li>
                        <div class="change">→ Improve album editing and photo upload UX</div>
                        <div class="details">Refactored album modification to separate info and photo uploads, providing user feedback during the process. Added a summary UI for upload progress, improved error handling, and updated backend routes and logic to support these changes. Enhanced album thumbnail and date range display in the template, and added
                            fallback bounds for albums without geolocated photos.
                        </div>
                    </li>
                    <li>
                        <div class="change">→ Add album modification form and backend support</div>
                        <div class="details">Introduces a new form and UI for modifying album details and uploading additional photos. Updates AlbumController, Album entity, and ImageService to handle album updates and image processing. Refactors frontend JS and SCSS for the new modify-album dialog, and updates translations and templates accordingly.</div>
                    </li>
                    <li>
                        <div class="change">→ Add summary section for series updates in admin</div>
                        <div class="details">Introduces a summary display for series updates in the admin interface. Updates JavaScript logic to generate and append summary rows for items with updates, adds corresponding summary container in the Twig template, and adjusts styles to support the new layout and scrolling behavior.</div>
                    </li>
                    <li>
                        <div class="change">→ Add ROLE_USER access control to video routes</div>
                        <div class="details">Applied the IsGranted('ROLE_USER') attribute to the index and show actions in VideoController to restrict access to authenticated users.</div>
                    </li>
                    <li>
                        <div class="change">→ Add null checks for menu elements and improve safety</div>
                        <div class="details">Fixes a bug that prevented you from logging in.</div>
                        <div class="details">Added checks to ensure elements like multiSearchOptions and historyList exist before accessing their properties or methods. This prevents potential runtime errors when these elements are not present in the DOM.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>23ᵉ mise à jour : 01/08/2025</h2>
                <ul>
                    <li>
                        <div class="change">→ Add album show page with map and photo enhancements</div>
                        <div class="details">Implemented AlbumShow.js for album display logic, including diaporama and map integration. Updated AlbumController to provide album and photo data as arrays, and added date range calculation for albums. Enhanced Map.js to display photo markers. Updated album and series styles for improved layout and map popups.
                            Modified templates to support new album show features, including map, photo previews, and form improvements. Fixed repository save methods and added array conversion methods to Photo and Album entities.
                        </div>
                    </li>
                    <li>
                        <div class="change">→ Refactor album and photo display, add photo metadata</div>
                        <div class="details">Updated AlbumController to fetch user albums and their photos, and refactored album and photo display in templates for improved layout and metadata presentation. Added date, latitude, and longitude fields to Photo entity with corresponding getters/setters. Updated translations for new metadata labels.</div>
                    </li>
                    <li>
                        <div class="change">→ Add progress info to series updates in admin</div>
                        <div class="details">Displays progress, last update date, and duration for series updates in the admin panel. Updates backend to track update start/end times and calculate duration, with translation support for time units. Improves user feedback during batch update operations.</div>
                    </li>
                    <li>
                        <div class="change">→ Add album and photo entities with basic album UI</div>
                        <div class="details">Introduces Album and Photo entities, their repositories, and relations to User. Adds AlbumController with index and show actions, and new Twig templates for album listing and details. Updates styles and menu to support albums, and adds French translations for album-related UI.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>22ᵉ mise à jour : 31/07/2025</h2>
                <ul>
                    <li>
                        <div class="change">→ Add scroll-to-top/bottom controls to series updates</div>
                        <div class="details">Introduces 'go-to-top' and 'go-to-bottom' buttons for easier navigation in the admin series updates section. Refactors markup and styles to support these controls and updates the JS logic to handle scrolling. Also adds a 'since' field to update details for improved context on previous updates.</div>
                    </li>
                    <li>
                        <div class="change">→ Add view transition animations and refactor theme switching</div>
                        <div class="details">Introduces view transition animations in app.scss for smoother theme changes. Refactors Menu.js to use the View Transition API when available for theme switching, moving theme update logic to a new updateTheme method. Also improves code style and consistency in list item removal.</div>
                    </li>
                    <li>
                        <div class="change">→ Refactor series updates UI and image handling</div>
                        <div class="details">Improves the admin series updates interface by grouping update lines and details into separate divs for each series, and disables the update button during processing. Image URLs for posters and backdrops are now passed as low/high resolution pairs, and srcset attributes are used for responsive images. Removes
                            unused debug output and updates API/Controller to provide new image URL structure.
                        </div>
                    </li>
                    <li>
                        <div class="change">→ Enhance admin series updates with detailed change display</div>
                        <div class="details">Added detailed update display for series fields in the admin panel, including visual comparison for images and improved status handling. Updated backend to return field-level changes, image URLs, and more granular update statuses. Improved frontend and styles to support new update details and visual feedback.
                        </div>
                    </li>
                    <li>
                        <div class="change">→ Refactor image addition logic in SeriesController</div>
                        <div class="details">Introduced addSeriesImage() to encapsulate image creation, saving, and update logic. Replaced repeated code blocks with calls to this new method for posters, backdrops, and other image types, improving maintainability and reducing duplication.</div>
                    </li>
                    <li>
                        <div class="change">→ Handle null providerId in getProvider method</div>
                        <div class="details">Adds a check for null providerId in the getProvider method, returning a default provider array when providerId is not set. This prevents potential errors when providerId is missing.</div>
                    </li>
                    <li>
                        <div class="change">→ Add admin series updates check feature</div>
                        <div class="details">Introduces a new admin tool for checking series updates, including a new JS module, controller route, repository method, and UI components. Updates styles and translations to support the new feature and integrates it into the admin interface.</div>
                    </li>
                    <li>
                        <div class="change">→ Refactor vote graph layout and structure</div>
                        <div class="details">Reorganized the vote graph in both SCSS and Twig to wrap episode votes in a new .episode-votes container, updated flex direction to row-reverse, and improved styling for vote-average. This enhances the visual structure and maintainability of the vote graph component.</div>
                    </li>
                    <li>
                        <div class="change">→ Add average vote display for season episodes</div>
                        <div class="details">Introduces a vote average section in the season view, showing the mean user vote for episodes. Updates JavaScript to recalculate and display the average dynamically, adds related styles, and provides translation for 'No votes'.</div>
                    </li>
                    <li>
                        <div class="change">→ Multi search: introduce search options WIP</div>
                    </li>
                    <li>
                        <div class="change">→ Prevent closing multi-search options on click</div>
                        <div class="details">Updated the click event handler to avoid closing the multi-search options dropdown when it is clicked. This improves user experience by allowing interaction with the multi-search options without unintentionally closing the menu.</div>
                    </li>
                    <li>
                        <div class="change">→ Improve update list layout and add change details</div>
                        <div class="details">Enhanced the user update list styles to better handle items with change details, including new gap and alignment rules for .change and .details elements. Updated the updates.html.twig template to display detailed descriptions for each change in the update list, improving clarity and user experience.</div>
                    </li>
                    <li>
                        <div class="change">→ Add opacity and transition to custom stills panel</div>
                        <div class="details">Sets default opacity and transition for the custom stills panel and ensures the .custom-stills element is fully opaque. Improves visual feedback and animation when toggling panel visibility.</div>
                    </li>
                    <li>
                        <div class="change">→ Replace h2 with header in series templates</div>
                        <div class="details">Updated series-in-a-while, series-to-start, and up-coming-series templates to use instead of for section headers containing search input. Adjusted related SCSS for spacing to improve layout consistency.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>21ᵉ mise à jour : 20/07/2025</h2>
                <ul>
                    <li>
                        <div class="change">→ Update theme color meta tag on navbar color change</div>
                        <div class="details">Added methods to convert HSL to hex and update the theme-color meta tag when the navbar color changes, ensuring browser UI matches the selected navbar color.</div>
                    </li>
                    <li>
                        <div class="change">→ Refactor add location form rendering in SeriesController</div>
                        <div class="details">Changed the controller to pass form data as an array instead of rendered HTML to the template. Updated the template to include the form using the data array, improving separation of concerns and maintainability. Also, the cooperative gestures toggle is now only shown when locations exist.</div>
                    </li>
                    <li>
                        <div class="change">→ Refactor season overview layout and styles</div>
                        <div class="details">Updated the structure and styling of season overview sections in both SCSS and Twig templates. Introduced new 'body' containers, improved the display of localized and additional overviews, and enhanced tool visibility and layout for source and locale indicators. Also commented out the scroll-timeline JavaScript
                            import.
                        </div>
                    </li>
                    <li>
                        <div class="change">→ Improve episode list layout and hover effect</div>
                        <div class="details">Added transition to width for smoother resizing, adjusted gap and padding for episode wrappers, and modified .infos width on hover for better UI responsiveness on screen less than 1024px wide.</div>
                    </li>
                    <li>
                        <div class="change">→ Add season localized overview support</div>
                        <div class="details">Introduces retrieval and display of season-specific localized overviews in the SeriesController and template. Updates styling for overview sections and translation keys. Refactors variable names for clarity and consistency.</div>
                    </li>
                    <li>
                        <div class="change">→ Refactor movie provider display and logic</div>
                        <div class="details">Updated provider handling in MovieController to support dynamic country selection and separate flatrate, buy, and rent providers. Improved provider display in movie templates for better clarity and layout, and added new French translations for provider types.</div>
                    </li>
                    <li>
                        <div class="change">→ Update vote graph layout and conditional rendering</div>
                        <div class="details">Adjusted the vote graph styles for improved spacing and responsiveness in series.scss. Refactored season.html.twig to render the vote graph only if episodes have user votes, and simplified the width calculation for episode votes.</div>
                    </li>
                    <li>
                        <div class="change">→ Refactor episode vote graph layout and styles</div>
                        <div class="details">Updated the vote graph in season.html.twig to wrap each vote in an 'episode-vote' container and display the episode number. Refactored and improved related SCSS for better structure, alignment, and visual clarity of vote bars and episode numbers.</div>
                    </li>
                    <li>
                        <div class="change">→ Include crew in preferred names lookup</div>
                        <div class="details">Added crew member IDs to the list of people for preferred name resolution in the SeriesController. This ensures that preferred names are retrieved for cast, guest stars, and crew.</div>
                    </li>
                    <li>
                        <div class="change">→ Enhance 'known for' display and styling for people</div>
                        <div class="details">Improves the logic for generating the 'known for' list in PeopleController, including correct handling of user-added media and poster URLs. Updates the SCSS to visually highlight user-added posters and adjusts poster sizing and border radius. The Twig template now adds a CSS class for user-added items, enabling
                            the new styling.
                        </div>
                    </li>
                    <li>
                        <div class="change">→ Update provider selection UI and tooltip logic</div>
                        <div class="details">Adjusted SCSS to unify styles for .select-provider and .select-vote, ensuring consistent display and sizing. Updated the season.html.twig template to show the provider name as the tooltip when a provider is selected, and 'Choose a provider' otherwise.</div>
                    </li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>20ᵉ mise à jour : 14/07/2025</h2>
                <ul>
                    <li>
                        <div class="change">→ Series page: Scope .content styles to elements with .left child</div>
                        <div class="details">Updated the selector from .content to .content:has(.left) to ensure flex-direction: column only applies when a .left child is present. This prevents unintended styling of .content elements without a .left child.</div>
                    </li>
                    <li>
                        <div class="change">→ Season page: Add episode vote graph and stats to season view</div>
                        <div class="details">Introduces a visual vote graph for episodes in the season view, displaying user and average votes. Also adds a new episode-stats section to show runtime and vote averages, with corresponding style updates and JavaScript logic to update the graph dynamically.</div>
                    </li>
                    <li>
                        <div class="change">→ Season page: Update series.scss for vote select and button visibility</div>
                        <div class="details">Set .select-vote to use flex display for improved layout. Hide .vote-button when it contains an SVG by adding a :has(svg) selector.</div>
                    </li>
                    <li>
                        <div class="change">→ People page: Improve preferred name form layout and styling</div>
                        <div class="details">Set width: 100% on several elements in the preferred name form for better alignment and responsiveness. Wrapped name in a span in the Twig template to allow for improved styling and text overflow handling.</div>
                    </li>
                    <li>
                        <div class="change">→ Map: Last additions: Add map centering feature for locations in UI</div>
                        <div class="details">Introduces a new UI control allowing users to center the map on a specific location from the last creations list. Updates Map.js to handle clicks on the new control, fly to the selected location, and open its marker popup. Styles and markup are updated to support the new feature.</div>
                    </li>
                    <li>
                        <div class="change">→ Improve series by country display and localization</div>
                        <div class="details">Refactors series-by-country UI to use a header element, updates styles for better visual grouping, and enhances the display of localized series names. Adjusts repository and controller logic to separate and render localized names, and updates template includes and variable handling for consistency.</div>
                    </li>
                    <li>
                        <div class="change">→ Add source fields to filming location forms and entity</div>
                        <div class="details">Introduces 'source-name' and 'source-url' fields to the filming location form, updates the FilmingLocation entity and its constructor to store these values, and ensures they are handled throughout the SeriesController. Also adds related styling classes and updates the form field rendering to support custom
                            classes.
                        </div>
                    </li>
                    <li>
                        <div class="change">→ Video page: Add video sharing feature with UI and translations</div>
                        <div class="details">Introduces a share button on the video page, allowing users to copy the video link to the clipboard. Adds a new share route and controller method, a dedicated share template, related styles, and French and Korean translations for share-related messages.</div>
                    </li>
                    <li>
                        <div class="change">→ Handle auxclick events and fix SVGs in Menu.js</div>
                        <div class="details">Added an event listener for 'auxclick' to close menus and clear search results when a middle mouse button is used. Also removed redundant fill="currentColor" attributes from SVG icon strings and ensured data-id is set as a string.</div>
                    </li>
                    <li>→ Filming locations: radius added to coordinates</li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>19ᵉ mise à jour : 23/06/2025</h2>
                <ul>
                    <li>→ Image service: code factoring & Apple Maps mark up</li>
                    <li>→ People preferred name: new input field</li>
                    <li>→ Episode schedules: remaining / elapsed time</li>
                    <li>→ Sass: nested declarations have to placed after</li>
                    <li>→ Remove user series</li>
                    <li>→ Series crew with multiple jobs</li>
                    <li>→ Schedules: number of episodes by selected day taken in count</li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>18ᵉ mise à jour : 16/06/2025</h2>
                <ul>
                    <li><span class="feature">{{ ux_icon('mdi:feature-highlight') }}</span>→ Site updates pages</li>
                    <li><span class="feature">{{ ux_icon('mdi:feature-highlight') }}</span>→ Series schedules: first week: selected days, then one weekly</li>
                    <li><span class="feature">{{ ux_icon('mdi:feature-highlight') }}</span>→ Points of interest: categories (buildings, towers, monuments,...)</li>
                    <li><span class="feature">{{ ux_icon('mdi:feature-highlight') }}</span>→ Videos: categories (environment, climate change, politics,...)</li>
                    <li><span class="improvement">{{ ux_icon('mi:arrow-right-up') }}</span>→ Unified location form (filming locations & poi)</li>
                    <li><span class="feature">{{ ux_icon('mdi:feature-highlight') }}</span>→ Map: cooperative gestures option</li>
                    <li><span class="improvement">{{ ux_icon('mi:arrow-right-up') }}</span>→ Admin poi: add poi form</li>
                    <li><span class="feature">{{ ux_icon('mdi:feature-highlight') }}</span>→ Map - last adds: points of interest displayed</li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                <h2>17ᵉ mise à jour : 10/06/2025</h2>
                <ul>
                    <li><span class="fix">{{ ux_icon('mdi:auto-fix') }}</span>→ Season page: quick episode links fixed</li>
                    <li><span class="feature">{{ ux_icon('mdi:feature-highlight') }}</span>→ Admin: videos added<span class="wip">{{ ux_icon('tabler:progress') }}</span></li>
                    <li><span class="feature">{{ ux_icon('mdi:feature-highlight') }}</span>→ Admin: points of interest added<span class="wip">{{ ux_icon('tabler:progress') }}</span></li>
                    <li><span class="feature">{{ ux_icon('mdi:feature-highlight') }}</span>→ User profile page: translation settings<span class="wip">{{ ux_icon('tabler:progress') }}</span></li>
                    <li><span class="improvement">{{ ux_icon('mdi:arrow-top-right') }}</span>→ Season page: paste an episode thumbnail→ displays the actual image file (was the pasted image)</li>
                    <li><span class="feature">{{ ux_icon('mdi:feature-highlight') }}</span>→ User Profile page: theme toggler</li>
                    <li><span class="feature">{{ ux_icon('mdi:feature-highlight') }}</span>→ Episode schedules: "Selected days" frequency</li>
                    <li><span class="improvement">{{ ux_icon('mdi:arrow-top-right') }}</span>→ Videos page: better display on small devices</li>
                    <li><span class="css">{{ ux_icon('mdi:language-css3') }}</span>→ css corrections here and there...</li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                16ᵉ mise à jour : 05/06/2025<br>
                <ul>
                    <li>→ Series crew css (no image profile)</li>
                    <li>→ Network & provider update command</li>
                    <li>→ Series network logos display fixed</li>
                    <li>→ Commands: Invokable Commands and Input Attributes - new in 7.3</li>
                    <li>→ Provider table removed</li>
                    <li>→ Mark / unmark episode as watched fixed</li>
                    <li>→ Twig Extension Attributes - new 7.3</li>
                    <li>→ Filming location images: Google Maps markup added</li>
                    <li>→ Filming location images: series name markup added</li>
                    <li>→ Series episode schedules: days of week→ episode count by days of week</li>
                    <li>→ sass deprecation: @import→ @use</li>
                    <li>→ Episode still: marked with "series name - SxxExx"</li>
                    <li>→ Admin: filming locations added</li>
                    <li>→ Map: ?fl=123→ goes directly to the location with series id=123</li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                15ᵉ mise à jour : 29/05/2025<br>
                <ul>
                    <li>→ Videos: sql optimizations, time to watch YT, pagination, categories</li>
                    <li>→ Symfony 7.3</li>
                    <li>→ Layout: footer outside the container</li>
                    <li>→ Movie search page (Menu movie)</li>
                    <li>→ Advanced search: country menu with flags</li>
                    <li>→ Video page: previous & next video</li>
                    <li>→ Error page</li>
                    <li>→ Advanced search: landscape orientation on iPad</li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                14ᵉ mise à jour : 25/05/2025<br>
                <ul>
                    <li>→ Advanced search: user series highlighted</li>
                    <li>→ People: responsiveness</li>
                    <li>→ Advanced search: filters display settings</li>
                    <li>→ Season page: cancel episode marked as viewed</li>
                    <li>→ The Movie Database: people names may need to be trim</li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                13ᵉ mise à jour : 23/05/2025<br>
                <ul>
                    <li>→ New video section (YouTube)</li>
                    <li>→ Series advanced search: new layout</li>
                    <li>→ Horizontal episode layout on xxl screen</li>
                    <li>→ Menu series history: new layout</li>
                    <li>→ New admin sections: series, movies, providers</li>
                    <li>→ Permanent main menu when window width >= 1920px</li>
                    <li>→ Season page: provider, device and vote can be canceled</li>
                    <li>→ Starred people: new layout (grid)</li>
                    <li>→ Series schedule "selected days" bug fixed</li>
                    <li>→ Series advanced search: persistent search settings</li>
                    <li>→ Season: first episode provider from watch links</li>
                    <li>→ Series advanced search: language, timezone & country added</li>
                    <li>→ Symfony UX (2.18.*→ 2.* - 2.25.2)</li>
                    <li>→ And many more ...</li>
                </ul>
            </div>
            <div class="site-update glowing-border">
                12ᵉ mise à jour : 11/05/2025<br>
                11ᵉ mise à jour : 08/05/2025<br>
                10ᵉ mise à jour : 23/04/2025<br>
                9ᵉ mise à jour : 19/04/2025<br>
                8ᵉ mise à jour : 18/04/2025<br>
                7ᵉ mise à jour : 16/04/2025<br>
                6ᵉ mise à jour : 09/04/2025<br>
                5ᵉ mise à jour : 08/04/2025<br>
                4ᵉ mise à jour : 07/04/2025<br>
                3ᵉ mise à jour : 05/04/2025<br>
                2ᵉ mise à jour : 04/04/2025<br>
                1ᵉ mise à jour : 25/09/2024<br>
            </div>
            <div class="site-update glowing-border">
                Ouverture : 27/10/2022<br>
                <div class="list">
                    27ᵉ mise à jour : 11/06/2024<br>
                    26ᵉ mise à jour : 25/01/2024<br>
                    25ᵉ mise à jour : 18/12/2023<br>
                    24ᵉ mise à jour : 25/10/2023<br>
                    23ᵉ mise à jour : 13/10/2023<br>
                    22ᵉ mise à jour : 11/10/2023<br>
                    21ᵉ mise à jour : 04/10/2023<br>
                    20ᵉ mise à jour : 28/06/2023<br>
                    19ᵉ mise à jour : 15/05/2023<br>
                    18ᵉ mise à jour : 27/04/2023<br>
                    17ᵉ mise à jour : 16/03/2023<br>
                    16ᵉ mise à jour : 12/03/2023<br>
                    15ᵉ mise à jour : 19/02/2023<br>
                    14ᵉ mise à jour : 10/02/2023<br>
                    13ᵉ mise à jour : 23/01/2023<br>
                    12ᵉ mise à jour : 12/01/2023<br>
                    11ᵉ mise à jour : 7/01/2023<br>
                    10ᵉ mise à jour : 6/01/2023<br>
                    9ᵉ mise à jour : 6/01/2023<br>
                    8ᵉ mise à jour : 4/01/2023<br>
                    7ᵉ mise à jour : 31/12/2022<br>
                    6ᵉ mise à jour : 19/12/2022<br>
                    5ᵉ mise à jour : 14/12/2022<br>
                    4ᵉ mise à jour : 8/12/2022<br>
                    3ᵉ mise à jour : 6/12/2022<br>
                    2ᵉ mise à jour : 22/11/2022<br>
                    1ᵉ mise à jour : 29/10/2022<br>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

